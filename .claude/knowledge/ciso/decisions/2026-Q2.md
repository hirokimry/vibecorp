# CISO 判断記録 2026-Q2




## 2026-05-08 — Issue #532 sync-check #2 再チェック（注釈追加後 + 0.33.6 互換遡及ロジック）

### 判定

**承認: セキュリティリスクなし**

### 対象

- `docs/ai-review-auth.md` — 冒頭への「enabled: false 運用中」注釈追加（前回 CISO 推奨 → 実施確認）
- `docs/ai-review-dependency.md` — `claude_action.enabled: false` 削除対象列挙更新 + 0.33.6 互換遡及ロジック説明追加
- `install.sh` — SHA256 完全一致を「管理下」とみなす遡及ロジック（`generate_ai_review_workflow` / `generate_ai_review_golden_test_workflow`）

### 各ファイル判定

**docs/ai-review-auth.md**: OK
- 前回 CISO 推奨の注釈（「Issue #532 で本体 enabled: false 中」）が冒頭に正確に追加済み
- ドキュメントの有効範囲（再有効化時 / 利用者向け参照資料として保持）が明記されており誤用リスクなし

**docs/ai-review-dependency.md**: OK
- `enabled: false` 時の削除対象に `ai-review-golden-test.yml` と `base snapshot` が追記済みで実装と整合
- 0.33.6 互換遡及ロジックの説明が追加済み。「ユーザー編集済みはハッシュ不一致で残置」の誤削除防止も明記

**docs/SECURITY.md**: OK（変更なし）
- 本 PR での差分なし。既存記述との整合も問題なし

**.claude/rules/autonomous-restrictions.md**: OK
- 本変更（workflow 削除方向）は #6「CI エージェント権限変更」の「追加・拡張」ではなく削除方向。抵触なし

### 遡及ロジック（SHA256 完全一致判定）のセキュリティ妥当性

compute_hash() は shasum -a 256 / sha256sum でブロック連鎖整合性を保証する標準ハッシュ関数を使用。偽陽性（ユーザー編集済みファイルを管理下と誤判定）リスク:
- SHA256 衝突確率は暗号学的に無視できる（2^-256）
- ユーザーが 1 文字でも変更すればハッシュ不一致 → 残置される
- 攻撃者が意図的に SHA256 を一致させるには pre-image 攻撃が必要（現実不可能）
- `compute_hash()` が空文字列を返した場合（ハッシュコマンド不在）は `was_managed` が false のまま → 誤削除しない（fail-safe）

攻撃経路: なし

### SECURITY.md 整合

変更なし。ai-review-auth.md への参照リンク（SECURITY.md §認証・認可）が引き続き有効。

### 過去判断との一貫性

2026-05-08 sync-check #1 の初回判断（「承認: セキュリティリスクなし。rm -f + 固定パス/内部関数戻り値に外部制御経路なし」）と完全一致。第二コミット追加後も脆弱性なし。

### 最終判断

承認: セキュリティリスクなし。全ドキュメント整合、autonomous-restrictions #6 との抵触なし、SHA256 遡及ロジックのセキュリティ妥当性確認済み。

---


## 2026-05-08 — Issue #532 claude-code-action 無効化 PR セキュリティ合議メタレビュー

### 判定

**Pass（条件なし承認）** — マージブロック不要。本 PR で修正不要。別 Issue 起票を推奨。

### 対象

- `install.sh` 新関数 `generate_ai_review_golden_test_workflow()` の `rm -f` 呼び出し
- `.github/workflows/ai-review.yml` の `claude_action.enabled: true → false` 切替
- intent: `intent/feature`

### 合議状況

- セキュリティチーム（security-analyst ×3）: 全員一致（Critical/High/Medium なし）
- 全会一致ルール: 適用なし（全員 Major 以上なし、Low/Minor 指摘のみ）
- 3/3 が同一箇所（`rm -f` の 2 段防御未適用）を Low/Minor 相当で指摘

### メタレビュー判定

**脆弱性（Major 以上）: なし**

`security-principles.md` の「rm -rf 安全パターン」はタイトル通り `rm -rf`（再帰削除）の 2 段防御を義務付けたもの。本 PR の対象は `rm -f`（単一ファイル削除）であり、同条項は直接の適用対象外。

加えて入力源の性質を確認する:
- `$base_snapshot`: 内部関数 `get_base_snapshot()` の戻り値。外部ユーザー入力ではない
- `$target`: `${REPO_ROOT}/.github/workflows/ai-review-golden-test.yml` 固定文字列。パストラバーサルの余地なし
- `$rel_path`: ハードコード文字列

既存の `generate_ai_review_workflow()` も同パターンで 2 段防御未適用。新関数はそれを踏襲しており、新規導入の脆弱性ではない。

**攻撃チェーン: リスクなし**

`rm -f` + 固定パス or 内部関数戻り値の組み合わせで、外部攻撃者が削除対象を制御できる経路が存在しない。

**SECURITY.md 整合: 問題なし**

`rm -rf` 安全パターンの条項を `rm -f` に拡張解釈する根拠なし。適用範囲外。

**見落とし: なし**

本 PR の主目的（claude-code-action 無効化）による攻撃面の変化を確認:
- `id-token: write` 権限参照経路: 削除方向（攻撃面減少）
- `CLAUDE_CODE_OAUTH_TOKEN` secrets 参照: 削除方向（攻撃面減少）
- autonomous-restrictions.md #6（CI エージェント権限変更）: 追加・変更ではなく削除のため非該当

**過去判断との一貫性: 一貫**

Issue #318（install.sh symlink 問題）では「攻撃の成立にはリポジトリ改ざんが前提」として High → Minor 格下げを承認した判例あり。今回の入力源（内部関数戻り値・固定パス）はそれより限定的であり、より低リスクと判断できる。一貫している。

### 脅威評価

- 深刻度: 低
- 攻撃チェーン: `rm -f` 対象は内部関数戻り値または固定パスのみ。外部からの経路制御不可
- 影響範囲: `ai-review-golden-test.yml` ファイルの誤削除リスクのみ（機密情報・認証への影響なし）

### 最終判断

**承認: セキュリティリスクなし**

- PR マージブロック: 不要
- 本 PR での修正: 不要（intent/feature の重視軸外、Minor 相当）
- 別 Issue 起票: 推奨（`install.sh` の `rm -f` に対して `${var:?}` ガードを適用するコードベース統一）
- PR コメント投稿: 不要（Major 以上の指摘なし）

### 根拠リンク

- `.claude/knowledge/security/security-principles.md` — rm -rf 安全パターン（rm -f への適用範囲外を確認）
- `.claude/rules/review-handling.md` — Minor 以下 + intent/feature 重視軸外 → 管轄外
- `decisions/2026-Q2.md` 2026-04-18 — Issue #318 の格下げ判例（攻撃前提確認の手法）


## 2026-05-08 — Issue #311 Phase 3: install.sh Linux bwrap 統合 + Win 弾き + ドキュメント整備

### 判定

承認（条件付き: Minor 指摘 2 点を対応推奨、修正必須なし）

### 対象

- `install.sh` — Linux bwrap 検出ロジック追加、Win/unknown エラーメッセージに doc 参照追加
- `tests/test_install_isolation_linux.sh` — 新規（macOS 上で Linux mock するテスト）
- `docs/SECURITY.md` — 脅威モデル表 3 段追加
- `docs/design-philosophy.md` — OS サポート節追加
- `docs/specification.md` — 隔離レイヤ節追加
- `README.md` — プリセット表に隔離列追加

### 合議状況

- Analyst 1: 総合判定 問題なし（Minor 1件 + Info 2件）
- Analyst 2: 総合判定 問題なし（Minor 2件）
- Analyst 3: 総合判定 要修正 M-01（Major 1件 + Minor 3件）
- 結果: 分裂（2:1、Analyst 3 のみ Major を検出）
- 全会一致ルール: Analyst 3 が Major を検出 → メタレビューで偽陽性精査実施

### Major 指摘 (Analyst 3 M-01) の評価

指摘内容: `VIBECORP_OS_RELEASE_PATH` 環境変数で任意ファイルを読み込める。

実装の実態:
- 読み込み後の処理は `awk -F= '$1=="ID"...'` フィールド抽出のみ
- 抽出結果は `case " ${distro_id} ${distro_id_like} " in` のパターンマッチで使用される
- このパターンマッチで実行されるのは `log_error` メッセージの出力のみ。ファイル削除・ネットワーク送信・eval 等は一切ない
- 任意ファイルを指定しても最悪のシナリオは「awk がパース失敗してデフォルト表示にフォールバック」のみ

偽陽性判定: Major（「機能・パフォーマンスに大きく影響する重要な問題」）には該当しない。Minor 相当と再判定。全会一致ルールのエスカレーション不要。

### 採用する指摘

| ID | 共通出典 | severity 再判定 | 内容 | 修正必須? |
|----|---------|----------------|------|----------|
| 共通-A | Analyst1 P311-M-001 / Analyst3 m-04 | Minor | 脅威 #2 二次防御欄「macOS: sandbox-exec で ~/.claude を read-only 化」が実装と矛盾（実際は Phase 1 既知制約: ~/.claude 全 RW 許可）。「WORKTREE 書込・~/.ssh deny、Phase 1 既知制約: ~/.claude は全 RW 許可」への修正推奨 | 対応推奨（修正必須でない） |
| 共通-B | Analyst1 P311-L-001 / Analyst2 M-001 | Minor | `VIBECORP_OS_RELEASE_PATH` の用途（テスト用 mock 機構）をコメントで明示していない | 対応推奨（修正必須でない） |

### 却下する指摘

| ID | 理由 |
|----|------|
| Analyst 3 M-01 の Major 格付け | log_error 表示選択のみで実害なし。偽陽性 |
| Analyst 2 M-002 (case 意図コメント) | 前後スペース境界技法は標準パターン。Trivial 相当で対応不要 |
| Analyst 3 m-02 (Phase 2 PATH hijacking) | 今回変更では実害なし。Phase 2 設計時対処と明記済み。管轄外 |
| Analyst 1/3 サプライチェーン脅威モデル明示 | intent/feature 重視軸外。別 Issue が適切 |

### 攻撃チェーン評価

`VIBECORP_OS_RELEASE_PATH` → awk パース → case パターン → log_error 表示選択 → exit 1。
攻撃者が任意ファイルを指定しても、ユーザー画面に表示されるのは distro 別インストールコマンドのテキスト変化のみ。情報漏洩・コード実行・権限昇格につながる経路なし。

過去判断との一貫性: 2026-04-16 Issue #318 Phase 3a（install.sh macOS 隔離レイヤ配置ロジック）でも同様の install.sh 変更を「条件付き承認」で通した実績あり。今回の Linux bwrap 追加は同一設計思想の延長線上にあり矛盾なし。SECURITY.md 脅威 #2 記述ズレ（共通-A）は既存の「Phase 1 既知制約: ~/.claude 全 RW 許可」節との矛盾であり、今回の脅威モデル表新規追加時に整合させるべきだった。

深刻度: 低（攻撃面拡大なし、機能追加のみ）

## 2026-05-06 — 不可領域チェック: claude-code-action が全 thread resolved 後に approve を発行する Step 5-A 追加（REVIEW.md / templates/REVIEW.md.tpl）

### 判定

**OK** — 不可領域 6 分類のいずれにも該当しない。自律実行起票を通過。

### 対象

- `REVIEW.md`（claude-code-action のプロンプト）
- `templates/REVIEW.md.tpl`（テンプレート元）
- 変更内容: Step 5-A として「GraphQL で全 thread isResolved を取得し、unresolved 0 件 + 新規修正対象 0 件なら `gh pr review --approve` を発行」を追加
- 明示的に変更しないもの: `ai-review.yml`、permissions / secrets / トリガー / Fork PR 除外条件 / GitHub App 権限スコープ

### 不可領域 6 分類 照合結果

| 分類 | 判定 | 根拠 |
|------|------|------|
| 認証 | 非該当 | hooks / settings.json permissions セクション不変 |
| 暗号 | 非該当 | secret/credential/token の扱い変更なし |
| 課金構造 | 非該当 | `gh pr review` は LLM 呼び出しではない。コスト上限パラメータ不変 |
| ガードレール | 非該当 | protect-files.sh / diagnose-guard.sh 不変 |
| MVV | 非該当 | MVV.md 不変 |
| CI エージェント権限 | 非該当 | ai-review.yml の permissions / secrets / トリガー / Fork PR 除外条件 / GitHub App 権限スコープをすべて変更しない。変更先はプロンプト文字列ファイルのみ |

### 攻撃チェーン分析

- `gh pr review --approve` を発行できる権限は ai-review.yml が既に保有しており、新たな権限スコープの追加はない（既存権限の行使条件をプロンプトで追加定義するだけ）
- `gh api graphql` は `Bash(gh api:*)` の allowedTools に既に含まれており、新規権限追加なし
- Fork PR では ai-review.yml の既存 Fork PR 除外条件により claude-code-action 自体が起動しないため攻撃面への影響なし
- プロンプトインジェクション経由で approve 条件を偽造する理論的経路は存在するが、CEO アカウント侵害が前提条件となるため信頼境界の設計上スコープ外（SECURITY.md 既知制約と同水準）

### 深刻度

低

### 過去判断との一貫性

CI エージェント権限の不可領域基準（autonomous-restrictions.md §6）は ai-review.yml の permissions/secrets/トリガー/Fork PR 除外条件/GitHub App 権限スコープの変更を対象とする。今回の変更はプロンプト文字列ファイルに限定されており、過去判断との矛盾なし。


## 2026-05-06 — 不可領域チェック: claude-code-action がレビューコメントを出さずにサイレント終了する問題を解消する（REVIEW.md 指示書形式への書き換え）

### 判定

OK — 自律実行不可領域 6 分類のいずれにも該当しない。

### 対象

- Issue「claude-code-action がレビューコメントを出さずにサイレント終了する問題を解消する」
- 修正方針: `REVIEW.md` をルールブック形式から指示書（差分確認 → 修正対象判定 → コメント投稿 → approve/request_changes 発行の明示的手順）形式に書き換える
- CEO（hirokimry）起票承認済み（PR #520 でレビュー欠落を観測した上での根本対応）

### 不可領域 6 分類の照合

| 分類 | 判定 | 根拠 |
|------|------|------|
| 1. 認証 | 非該当 | `hooks/*auth*.sh` / `hooks/*permission*.sh` / `settings.json permissions` / `gh auth` / `ANTHROPIC_API_KEY` のいずれにも触れない。`REVIEW.md` はプロンプト文字列であり認証機構ではない |
| 2. 暗号 | 非該当 | `encrypt` / `decrypt` / `secret` / `credential` / `token` を扱うコードの変更なし |
| 3. 課金構造 | 非該当 | `docs/cost-analysis.md` / `ANTHROPIC_API_KEY` 参照箇所 / コスト上限パラメータ / LLM 起動方式のいずれも変更しない。コスト単価変動は「プロンプト明確化の副作用（バグ修正）」であり不可領域 3 の定義する構造変更に非該当 |
| 4. ガードレール | 非該当 | `protect-files.sh` / `diagnose-guard.sh` / `/vibecorp:diagnose forbidden_targets` / diagnose-active スタンプ制御のいずれにも触れない |
| 5. MVV | 非該当 | `MVV.md` への変更なし |
| 6. CI エージェント権限 | 非該当 | `permissions` / `secrets` 参照 / トリガー / Fork PR 除外条件 / GitHub App 権限スコープを変更しない。不可領域 6 の定義は「権限の変更」に限定されており、エージェントに渡すプロンプト内容の変更は対象外 |

### 攻撃チェーン観点

`REVIEW.md` はプロンプト文字列であり、以下の理由から攻撃経路として悪用できる要素は存在しない:
- 認証情報へのアクセスを指示する変更ではない
- Fork PR 向けの動作はワークフロー側の `if: github.event.pull_request.head.repo.full_name == github.repository` 条件でプロンプト内容に影響されず制御される
- 指示書形式への書き換えは prompt injection 耐性を下げる方向ではない

### 過去判断との一貫性

インデックスに `REVIEW.md` に関するエントリなし（Issue #465「REVIEW.md prompt 引き渡し経路メタレビュー」は異なる観点であり今回と競合しない）。一貫。

### 脅威評価

- 深刻度: 低（不可領域 6 分類に非該当）
- 影響範囲: `REVIEW.md` のプロンプト内容のみ。ワークフロー・secrets・権限スコープ不変


## 2026-05-06 — 不可領域チェック: PR_NUMBER 環境変数が渡されずレビューが空振りする問題を解消する（ai-review.yml env: セクション追加）

### 判定

OK — 自律実行不可領域 6 分類のいずれにも該当しない。

### 対象

- Issue: `fix: claude-code-action 実行時に PR_NUMBER 環境変数が渡されずレビューが空振りする問題を解消する`
- 変更内容: `.github/workflows/ai-review.yml` の Claude Code Action 実行ステップに `env:` セクション（`PR_NUMBER`、`GITHUB_REPOSITORY`）を追加
- 変更しない要素: `permissions` ブロック、`secrets` 参照、トリガー、Fork PR 除外条件、GitHub App 権限スコープ
- CEO（hirokimry）手動起票済み（PR #520 でサイレント問題を観測した上での根本対応）

### 不可領域 6 分類の照合

| 分類 | 判定 | 根拠 |
|------|------|------|
| 1. 認証 | 非該当 | `hooks/*auth*.sh` / `hooks/*permission*.sh` / `settings.json permissions` / `gh auth` / `ANTHROPIC_API_KEY` のいずれにも触れない |
| 2. 暗号 | 非該当 | `encrypt` / `decrypt` / `secret` / `credential` / `token` を扱うコードの変更なし |
| 3. 課金構造 | 非該当 | `ANTHROPIC_API_KEY` 参照方式の変更なし、LLM 起動コマンド変更なし |
| 4. ガードレール | 非該当 | `protect-files.sh` / `diagnose-guard.sh` 変更なし |
| 5. MVV | 非該当 | `MVV.md` 変更なし |
| 6. CI エージェント権限 | 非該当 | `ai-review.yml` を修正するが `env:` 追加のみ。`permissions` / `secrets` / トリガー / Fork PR 除外条件 / GitHub App 権限スコープのいずれも変更しない |

### 攻撃チェーン分析

`PR_NUMBER` は `${{ github.event.pull_request.number }}` 等の GitHub Actions context 値。`GITHUB_REPOSITORY` も GitHub が自動設定する標準環境変数。両値とも公開情報であり、`env:` で子プロセスに渡すことは新規の権限付与ではなく既存 context 値の伝播に過ぎない。Fork PR 攻撃経路において `PR_NUMBER` / `GITHUB_REPOSITORY` を知ることに攻撃上の意味はない（secrets 漏洩経路の変化なし）。

### 過去判断との一貫性

Issue #505 の `id-token: write` 例外承認と同様に、動作要件の整備であり権限スコープの拡大ではない。一貫性あり。

### 最終判断

OK — 人間承認ルート（CEO 手動起票）に従って実装可能。

---


## 2026-05-06 — 不可領域チェック: PR 作成時 intent ラベル数チェックのレースコンディション修正 Issue

### 判定

OK（6分類いずれにも非該当）

### 対象

- `.github/workflows/ai-review.yml` — `intent-label-check` ジョブと `inherit-intent`（`pr-intent-inherit.yml`）の並列実行による競合状態の解消

### 不可領域 6 分類チェック

| 分類 | 該当性 | 根拠 |
|------|--------|------|
| 1. 認証 | 非該当 | 認証・認可フック・permissions セクション変更なし |
| 2. 暗号 | 非該当 | encrypt/decrypt/credential/token 操作なし |
| 3. 課金構造 | 非該当 | LLM 起動方式・コスト上限パラメータ変更なし |
| 4. ガードレール | 非該当 | protect-files.sh / diagnose-guard.sh / forbidden_targets / diagnose-active スタンプ変更なし |
| 5. MVV | 非該当 | MVV.md 変更なし |
| 6. CI エージェント | 非該当（詳細下記） | permissions ブロック・secrets 参照・トリガー追加・Fork PR 除外条件・GitHub App 権限スコープ変更なし |

### §6 CI エージェント詳細評価

`ai-review.yml` 現状（確認済み）:
- `permissions`: `contents: read / pull-requests: write / issues: write / id-token: write`（Issue #505 CISO 承認済み最小権限構成）
- `on.pull_request.types`: `[opened, synchronize, ready_for_review]`（変更対象外）
- Fork PR 除外条件: `github.event.pull_request.head.repo.full_name == github.repository`（変更対象外）
- `secrets`: `CLAUDE_CODE_OAUTH_TOKEN / GITHUB_TOKEN` 参照（変更対象外）

Issue 本文が修正対象として挙げる変更は「`intent-label-check` ジョブへの `needs:` 依存関係追加」または「`labeled/unlabeled` イベントでの再走」。いずれも:
- `permissions` ブロックを変更しない
- `secrets` 参照を変更しない
- トリガー種別（`pull_request` のまま、`pull_request_target` への変更なし）
- Fork PR 除外条件を緩和しない
- GitHub App 権限スコープを変更しない

「`labeled/unlabeled` イベント追加」の場合のみ §6 に抵触する可能性を精査: `on.pull_request.types` に `labeled/unlabeled` を追加することは **許可トリガー変更ではあるが**、`pull_request_target` への変更・Fork PR 除外条件削除・secrets 流出経路拡大のいずれにも該当しない。`labeled/unlabeled` も `pull_request` スコープ内であり Fork PR では secrets が渡らない保護は維持される。不可領域 §6 の列挙する禁止事項（`pull_request_target` 変更・Fork PR 除外削除・禁止権限スコープ追加）には非該当。

### 攻撃チェーン観点

- 変更はジョブ順序（依存関係・タイミング）のみ
- secrets 流出経路・権限昇格経路の新規追加なし
- レースコンディション修正により「ラベル継承前に数チェックが走り意図せず fail」という CI 誤判定が解消される

### 過去判断との一貫性

- 2026-05-04 Issue #461（ai-review.yml 新規追加）: `permissions` 最小権限・Fork PR 除外・secrets 最小参照の確認済み構成を前提としており、本 Issue はその構成を維持したまま動作品質を改善する。矛盾なし。
- 2026-05-04 Issue #465（REVIEW.md prompt 引き渡しメタレビュー）: claude-review ジョブの前提条件として `intent-label-check` の `needs:` 依存がすでに実装済みであることを確認（行 69: `needs: intent-label-check`）。今回の Issue は pr-intent-inherit（別ワークフロー）との間の競合を解消するもので、既存 needs 構造との整合性あり。

### 結論

自律実行 OK。不可領域 6 分類いずれにも非該当。起票・/ship 実行可。

---


## 2026-05-06 — 不可領域チェック: Issue 🔒 fix: 配布物の auto-approve / コマンドログ / sandbox のセキュリティ穴を塞ぐ

### 判定

OK — 不可領域 1〜6 のいずれにも該当しない。自律実行対象として起票可能。

### 対象

- `team-auto-approve.sh`: `.claude/hooks/*`、`.claude/settings.json`、`.github/workflows/*` を Write/Edit 保護対象に追加（保護強化）
- `team-auto-approve.sh`: `*.env` → `*.env*` glob 拡張（`.env.local`、`.env.production` 等を保護対象追加）
- `command-log.sh`: `--token`、`KEY=`、`ANTHROPIC_API_KEY=`、`sk-*`、`ghp_*` 等をマスキング
- `claude.sb`: `(allow process-exec)` 無制限 → `/usr/`、`/opt/homebrew/`、`/bin/`、`/sbin/` 等に制限

### 各修正の不可領域照合

**修正 1（team-auto-approve.sh 保護対象追加）**

- ガードレール（4番）: team-auto-approve.sh は protect-files.sh / diagnose-guard.sh / forbidden_targets のいずれでもない。保護対象を増やす方向の変更であり、ガードレール制御ロジック自体は変更しない。
- 認証（1番）: settings.json の permissions セクションを変更するのではなく、auto-approve フックが settings.json を「保護対象として自動承認しない」ようにする変更。認証・認可の挙動を変えない。
- 判定: OK

**修正 2（.env glob 拡張）**

- 暗号（2番）: 暗号処理コードの変更ではなく、.env ファイル保護の glob パターン拡張。機密情報を守る範囲を広げる強化方向。
- 判定: OK

**修正 3（command-log.sh マスキング）**

- 暗号・認証（1/2番）: ANTHROPIC_API_KEY の「扱い」変更は課金構造文脈（3番）との並記として読む。コマンドログへの混入を防ぐマスキングは機密情報保護の強化であり、認証挙動を変えない。
- 注意: マスキングパターン（`sk-*`、`ghp_*`）は prefix ベースのため、Base64 エンコード済みトークン等は検出できない可能性あり。実装後の security-analyst 合議レビューで確認すること。
- 判定: OK

**修正 4（claude.sb process-exec 制限）**

- ガードレール（4番）: claude.sb は sandbox-exec プロファイルであり、protect-files.sh 等ではない。sandbox 境界を強化する変更。
- 過去判断との整合: 2026-04-16 Issue #309 で「process-exec ホワイトリスト化は Phase 2 以降スコープ」とされていた既知制約の解消。矛盾なし。
- 判定: OK

### 攻撃チェーン分析

全 4 件が保護強化方向の変更であり、悪用される攻撃経路は存在しない。`.github/workflows/*` 保護追加は不可領域 6（CI エージェント）の防御を補完する。

### 深刻度

低（リスク低減方向の変更のみ）

### 過去判断との一貫性

一貫。Issue #309 Phase 2 スコープアウト項目（process-exec 制限）の解消として整合。


## 2026-05-06 — 不可領域チェック: Issue #360 子 Issue「配布スキル/フックの動作バグを一括修正」

### 判定

条件付き承認 — 8 件全て不可領域非該当またはCEO承認済みの人間承認ルート。自律実行対象（diagnose 起票フロー）としては #3（diagnose-guard.sh）のみ除外。CEO 手動起票 Issue の /ship 実装は許容。

### 対象

- Issue: 「配布スキル/フックの動作バグを一括修正（Issue #360 残課題）」、修正対象 8 件

### 照合結果

| 修正 | 内容 | 不可領域 | 判定 |
|------|------|---------|------|
| #1 | review-harvest jq 連結バグ修正 | 非該当 | OK |
| #2 | pr-review-loop 予算ガード追加（max-iterations=20 / duration=60min） | 非該当（上限新設=強化方向、「緩和」ではない） | OK |
| #3 | diagnose-guard.sh REGEX_PATTERN 末尾アンカー追加 | 不可領域 4 明示列挙対象 | CEO 承認済みで人間承認ルート通過 → 自律実行は除外、/ship は可 |
| #4 | role-gate.sh UUOC 修正 | 非該当 | OK |
| #5 | pr-review-fix ページネーション修正 | 非該当 | OK |
| #6 | audit-cost 2>/dev/null 違反解消 | 非該当 | OK |
| #7 | review-harvest 2>/dev/null 違反解消 | 非該当 | OK |
| #8 | test.yml cancelled 扱い + sandbox exit code 明確化 | 非該当（権限変更なし） | OK |

### 修正 #2 の課金構造判断詳細

`max-iterations` / `duration` パラメータは `autonomous-restrictions.md` 3番に列挙された `max_issues_per_day` / `max_issues_per_run` とは異なるパラメータ名。かつ上限を「新設」する方向（緩和ではない）のため課金構造の不可領域には非該当と判定した。今後 LLM 呼び出し回数を直接制御するパラメータを変更する場合は改めて課金構造照合が必要。

### 修正 #3 の人間承認ルート判断詳細

`diagnose-guard.sh` は不可領域 4（ガードレール）に名称で明示列挙されている。内容はガードレール強化方向（誤マッチを除去）であるが、ルールは方向の区別なく「変更すること自体」を不可領域と規定している。今回は CEO（hirokimry）が案 B エピック分解を明示承認しており、`autonomous-restrictions.md` の「人間承認ルート」（ユーザーが手動で Issue を起票して /ship で実装することは可能）に相当する。

### 攻撃チェーン評価

全修正が現状の防御を維持または強化する方向。#3 のバグ修正はマッチ精度の向上であり、攻撃者がガードレールを迂回する経路の追加には繋がらない。攻撃面の変化: なし。

### 深刻度

低

### 過去判断との一貫性

`autonomous-restrictions.md` における人間承認ルートの解釈（CEO が手動起票 = 自律実行制限の外）は 2026-04-22 判断（承認ダイアログ問題「除外」）と一貫している。

---


## 2026-05-06 — 不可領域チェック: /ship が PR 作成時に Issue から intent ラベルを継承するようになる

### 判定

OK — 自律実行不可領域 6 分類のいずれにも該当しない。起票を承認する。

### 対象

- `skills/ship/SKILL.md` — Issue から intent ラベルを取得して `gh pr create --label` に渡すロジック追加
- `.github/workflows/ai-review.yml` — intent-label-check ジョブの継承ステップ（PR #518 で誤追加）を撤去し、数チェックのみに戻す変更
- `.github/workflows/pr-intent-inherit.yml` — 削除維持（変更なし）

### 不可領域 6 分類との照合

| 分類 | 判定 | 根拠 |
|------|------|------|
| 認証 | 対象外 | `settings.json` permissions / `gh auth` / `ANTHROPIC_API_KEY` に変更なし |
| 暗号 | 対象外 | `encrypt` / `decrypt` / `secret` / `credential` / `token` を扱うコードに変更なし |
| 課金構造 | 対象外 | `claude -p` 等 LLM 起動方式、コスト上限パラメータに変更なし |
| ガードレール | 対象外 | `protect-files.sh` / `diagnose-guard.sh` / `forbidden_targets` / `diagnose-active` に変更なし |
| MVV | 対象外 | `MVV.md` 自体に変更なし |
| CI エージェント権限 | 対象外（詳細下記） | `permissions` / `secrets` / トリガー / Fork PR 除外条件 / GitHub App 権限スコープに変更なし |

#### CI エージェント権限の精査

`ai-review.yml` が変更対象に含まれるため精査した。変更内容は intent-label-check ジョブ内の「ラベル継承ステップ（`gh issue view` / `gh pr edit --add-label`）の削除」のみ。`permissions` セクション・`secrets` 参照・トリガーイベント・Fork PR 除外条件・GitHub App 権限スコープのいずれも変更しないことが Issue 本文に明記されており、不可領域定義の要件に該当しない。

むしろ CI ジョブ側でのラベル書き換えを ship スキル側（人間承認を経た実行経路）に移すことで、CI ジョブの権限スコープを縮小する方向の変更と評価できる。

### 攻撃チェーン分析

攻撃経路なし。`gh issue view` でラベルを取得し `gh pr create --label` に渡すシェル操作は、認証・暗号・権限昇格と無関係。CI エージェント権限の不可領域が求める「攻撃者がシークレットを窃取できる経路の開口」は存在しない。

### 過去判断との一貫性

一貫。CI エージェント権限に関する過去 CISO 判断（Issue #505 の `id-token: write` 例外承認）と矛盾なし。

---


## 2026-05-06 — Issue #513 command-log.sh 機密情報マスキング + claude.sb process-exec ホワイトリスト制限 管轄ドキュメント整合性チェック

### 判定

承認: セキュリティリスクなし

### 対象

- `templates/claude/hooks/command-log.sh` — 機密情報マスキング（SECRET_PATTERNS 配列 + mask_secrets()）
- `templates/claude/sandbox/claude.sb` + `.claude/sandbox/claude.sb` — process-exec ホワイトリスト制限
- `docs/SECURITY.md` — 既知制約表 process-exec 行の更新

### SECURITY.md と実装の整合性

SECURITY.md 行 139 のホワイトリスト列挙（`/usr`, `/bin`, `/sbin`, `/System`, `/Library`, `/opt/homebrew`, `/usr/local`, `/Applications`, `~/.local/share/claude`）と claude.sb 実装が完全一致。残存リスク（osascript 等システムバイナリ経由の境界迂回）も SECURITY.md に明記済み。整合性: 問題なし。

### マスクパターン評価

カバー済み: `sk-ant-*` / `ghp_*/ghs_*/gho_*/ghu_*/ghr_*` / `ANTHROPIC_API_KEY=` / `GH_TOKEN=` / `GITHUB_TOKEN=` / `OPENAI_API_KEY=` / `--token` / `--auth-token` / `--password` / `--secret` / `*_SECRET=` / `*_PASSWORD=`

軽微な見落とし（ブロック要因ではない）:
- `AWS_ACCESS_KEY_ID=`（`AKIA...` 形式）が `*_KEY=` パターン不在のためカバー外
- `--api-key=` がカバー外（`claude -p` 使用ケースで補完望ましい）

### 攻撃チェーン観点

- process-exec ホワイトリスト制限: WORKTREE 内の悪意バイナリ実行不可。CR-001 WORKTREE 境界封鎖との整合性確認済み。
- command-log マスキング: ログファイル経由の credential サイドチャネル漏洩を防止。

### 過去判断との一貫性

2026-04-16「Issue #309 macOS sandbox-exec PoC Phase 1」で process-exec 無制限を既知制約として認識済み。今回の制限付与はその解消であり矛盾なし。CR-001「WORKTREE ⊇ HOME 攻撃経路」封鎖の延長線上に位置する。

### 推奨事項（次 Issue）

`AWS_ACCESS_KEY_ID=` / `AKIA[A-Z0-9]{16}` 形式および `--api-key=` のマスクパターン追加を次 Issue で対応推奨。

---


## 2026-05-04 — Issue #468 sync-check 再実行: docs/ai-review-auth.md §5 検証ロジック表更新のメタレビュー

### 判定

承認: セキュリティリスクなし

### 対象

- `docs/ai-review-auth.md` §5「install.sh の secrets 検証」検証ロジック表
  - `claude_action` セクション不在（旧 vibecorp.yml）行の追記
  - 既存値上書き禁止の補足追加
- 関連実装: `install.sh` の `ensure_claude_action_section`、`verify_claude_action_secrets`
- 関連テスト: `tests/test_install_claude_action_yml.sh`（28 観点）

### 合議状況

- 前回（実装時）は security-analyst 全員一致 OK
- 今回はドキュメント更新後の再確認（ドキュメント単体変更）
- 全会一致ルール: ドキュメント整合更新につき脆弱性なし

### 整合性確認

**実装とドキュメントの一致**:
- セクション不在 → `cat >> "$yml"` で `enabled: true` 付きセクション末尾追記（実装 L1003-1021）
- 既存値上書き禁止: `has_enabled` / `has_skip_paths` の yes チェックで保護（実装 L1037-1039）
- `enabled: false` を明示したリポジトリは `has_enabled == "yes"` と評価され、`emit_missing()` 内で追記をスキップする

**攻撃チェーン（`enabled: false` の強制上書き）**:
- `ensure_claude_action_section` は `has_enabled == "yes"` の場合、`enabled` キーを追記しない
- `enabled: false` と明示したリポジトリが `--update` 後に `enabled: true` に変わる経路は存在しない
- リスクなし

**SECURITY.md 整合**:
- SECURITY.md の「認証・認可」セクションは ai-review-auth.md を Source of Truth とする参照構造
- §5 更新による SECURITY.md 変更は不要。参照先の更新で整合が維持される

### 脅威評価

- 深刻度: 低（ドキュメント整合更新のみ）
- 影響範囲: ドキュメント閲覧者の認識精度向上のみ（セキュリティリスクへの影響なし）

### 管轄ファイル判定

| ファイル | 判定 |
|---------|------|
| `docs/SECURITY.md` | OK（参照構造維持・変更不要） |
| `.claude/knowledge/ciso/` | OK（本エントリで記録） |


## 2026-05-04 — Issue #465 REVIEW.md prompt 引き渡し経路のメタレビュー

### 判定

条件付き承認: docs/ai-review-auth.md §3「ワークフロー構成」表への追記を条件とする（Minor）

### 対象

- `install.sh`: `generate_review_md` 新規追加 + `generate_coderabbit_yaml` の `path_filters` 注入
- `templates/REVIEW.md.tpl`（新規）: AI レビュー指示プロンプトテンプレート
- `templates/coderabbit.yaml.tpl`: `{{PATH_FILTERS_BLOCK}}` プレースホルダ化
- `templates/.github/workflows/ai-review.yml`: REVIEW.md を `$GITHUB_OUTPUT` 経由で claude-code-action の `prompt` として渡す実装
- `tests/test_install_review_md.sh`（新規）

### 合議状況

- 合議なし（CISO 単独判断）
- 全会一致ルール: 適用なし（合議未実施）

### 脅威評価

**深刻度**: 低（ドキュメント未更新のみ）

**EOF_REVIEW_MD heredoc 注入**:
- `_read_skip_paths` の出力は `printf -- '- "%s"\n' "$path"` でダブルクォートで囲まれるため、`EOF_REVIEW_MD` 単体行にはならない。heredoc 途中終端は現実的でない。
- REVIEW.md の `language` 展開も `**{{LANGUAGE}}**` 内に埋め込まれ単体行にならない。

**外部 prompt injection**:
- `if: github.event.pull_request.head.repo.full_name == github.repository` により Fork PR は ai-review.yml の全ジョブが除外される。外部攻撃者は REVIEW.md に到達不可。

**内部 collaborator による REVIEW.md 改ざん**:
- push 権限を持つ collaborator は REVIEW.md に悪意ある prompt を混入できる（信頼境界内のリスク）。
- 設計上許容（ai-review-auth.md §3「pull_request_target 不採用」と同様の信頼境界設計）。

### 必須対応（Minor）

`docs/ai-review-auth.md` §3「ワークフロー構成」表に以下 2 行を追記すること:

| 要素 | 値・条件 | 根拠 |
|---|---|---|
| `review_prompt` ステップ | REVIEW.md が存在すれば `EOF_REVIEW_MD` heredoc で `$GITHUB_OUTPUT` に書き出し、不在時は空値 | install.sh が vibecorp.yml の language / skip_paths から生成 |
| `claude-review` の `prompt` | `steps.review_prompt.outputs.prompt`（空値の場合は claude-code-action のデフォルト動作） | REVIEW.md が SoT、外部 Fork PR は if: 条件で到達不可 |

### SECURITY.md 整合性

SECURITY.md 本体への変更不要。認証経路は ai-review-auth.md を SoT として委譲済みの構造が維持されている。

### 過去判断との一貫性

2026-05-04「Issue #461 sync-check 再実行」で「ワークフロー構成」表を承認した時点では REVIEW.md prompt 引き渡しステップが未実装だった。Issue #465 で追加されたため、ai-review-auth.md の追記が必要。矛盾なし。

---


## 2026-05-04 — Issue #461 sync-check 再実行（docs/ai-review-auth.md §3 + ワークフロー構成節）

### 判定

承認: セキュリティリスクなし（前回指摘 2 点の解消を確認）

### 対象

- `docs/ai-review-auth.md` §3「防御的実装」セクション書き換え
- `docs/ai-review-auth.md` §3「ワークフロー構成」表の新設

### 合議状況

- 合議なし（CISO sync-check 再実行）
- 全会一致ルール: 適用なし（脆弱性検出なし）

### メタレビュー結果

**前回指摘1（必須・Minor）解消確認:**
`§3「明示的な if 条件は不要」→「防御的実装」セクションへの書き換え`。
`intent-label-check` / `claude-review` の両ジョブへの明示 if 条件（`github.event.pull_request.head.repo.full_name == github.repository`）が記載された。暗黙依存からの明示ゲートへの変更が正しく文書化されている。**解消済み。**

**前回指摘2（推奨・Minor）解消確認:**
`§3「ワークフロー構成」表`が新設され、`on.pull_request.types`、`permissions` 3スコープ、`concurrency` 2項目、`intent-label-check`、`claude-review` の8行が記載された。CISO最小権限（`contents: read`）が明文化されている。**解消済み。**

**新たなセキュリティ的乖離の有無:**
- `if:` 条件の式 `github.event.pull_request.head.repo.full_name == github.repository` は GitHub Actions 内部値であり攻撃者から改ざん不可。正当な防御。
- `permissions` 3スコープはいずれも最小権限設計として適切。
- OWASP Top 10（A01 相当）: `pull_request_target` 不採用理由が明示されており、設計意図が追跡可能。
- `concurrency.cancel-in-progress: true` は DoS耐性の観点でも問題なし。
- SECURITY.md整合: SoT 参照構造（SECURITY.md → ai-review-auth.md）を維持しており問題なし。

**攻撃チェーン:**
新たな攻撃経路は発生していない。前回評価した3経路（Fork→secrets 窃取 / プロンプトインジェクション / @v1 Supply Chain）の評価変更なし。

### 過去判断との一貫性

- 2026-05-04「Issue #461 ai-review.yml 新規追加のメタレビュー」の条件付き承認条件が本再実行で充足された。
- 任意事項（Trivial: @v1 コミットハッシュピン留め）は別 Issue 化推奨のまま変更なし。

---


## 2026-05-04 — Issue #461 ai-review.yml 新規追加のメタレビュー（CISO 直接評価）

### 判定

条件付き承認: docs/ai-review-auth.md の Fork PR 除外方式記述の更新を条件とする

### 対象

- `templates/.github/workflows/ai-review.yml`（新規）
- `install.sh` の `generate_ai_review_workflow` 関数追加
- `tests/test_install_ai_review_workflow.sh`（新規）

### 合議状況

- 合議なし（CISO 直接評価依頼）
- 全会一致ルール: 脆弱性検出なし（ドキュメント乖離は Minor）

### メタレビュー結果

**permissions 整合**: 問題なし。`contents: read` / `pull-requests: write` / `issues: write` の 3 スコープは最小権限原則と整合。`issues: write` は intent-label-check ジョブの `gh pr comment` に必要。

**Fork PR 除外**: コード実装は両ジョブに明示的 `if:` 条件を設置しており、より確実な防御。しかし `docs/ai-review-auth.md` セクション3「明示的な if 条件は不要」と実装が矛盾 → **ドキュメント更新必須（Minor）**。

**secrets 流出リスク**: 新規リスクなし。`CLAUDE_CODE_OAUTH_TOKEN` は Fork PR 除外後のジョブでのみ参照。`pull_request_target` トリガー不使用。ai-review-auth.md 方針に完全準拠。

**攻撃チェーン**:
- Fork PR → secrets 窃取: 除外条件により不成立
- プロンプトインジェクション: claude-code-action 側の問題。SECURITY.md「既知制約」内
- Supply Chain Attack（@v1 ピン留め不在）: Anthropic 公式リポジトリへの依存のため低リスク。Trivial 改善事項

### 修正指示

1. **必須（Minor）**: `docs/ai-review-auth.md` セクション3「明示的な if 条件は不要」を「両ジョブに明示的 if 条件を設置して確実に除外する」に改訂
2. **推奨（Minor）**: 同ドキュメントに intent-label-check ジョブ・concurrency・permissions 詳細を追記
3. **任意（Trivial）**: `@v1` コミットハッシュピン留めを将来 Issue として検討

### 過去判断との一貫性

- 2026-05-03「claude-code-action GitHub App 権限スコープ設計」条件付き承認の「最小スコープ制限」条件と一致（実装が条件を満たしている）
- 2026-05-04「Issue #468 ai-review-auth.md §5 更新」と同一ドキュメントへの修正要求であり整合


## 2026-05-03 — claude-code-action（GitHub App 認証）導入の権限スコープ設計 / autonomous-restrictions.md 該当性判定

### 判定

条件付き承認（5点の設計条件を満たすこと）

### 対象

- claude-code-action（GitHub App）の権限スコープ設計
- `.github/workflows/claude*.yml` のトリガー・secrets 管理設計
- `autonomous-restrictions.md` の更新要否

### autonomous-restrictions.md 該当性

認証領域には**直接は該当しない**（GitHub App は vibecorp の認証機構を変更しない）。ただし CI エージェントのカテゴリが現行定義の射程外であり、**新規カテゴリ追加が必要**。

### GitHub App 最小権限スコープ

```text
contents: read
pull-requests: write
issues: write
```

以下は付与禁止:
- `administration: write`（リポジトリ設定変更）
- `secrets: write`（GitHub Actions secrets RW）
- `workflows: write`（CI/CD 変更 = ガードレール迂回経路）

### 触れさせない範囲の確保

| 侵害経路 | 対策 |
|---------|------|
| `workflows: write` 付与 → hooks/settings.json 改ざん | スコープ制限で封鎖 |
| `pull_request_target` トリガー + secrets 参照 | トリガーを `pull_request` に限定（`pull_request_target` 使用禁止） |
| `CLAUDE_CODE_OAUTH_TOKEN` 漏洩 | GitHub Actions environment secrets（保護ルール付き）で管理。App に `secrets: read` を与えない |
| プロンプトインジェクション（同一リポジトリ PR） | Fork PR 除外は確定済み。同一リポジトリの悪意ある contributor からの注入リスクは残存（許容済み） |

### ガードレール hook 追加要否

既存の `protect-files.sh` / `protect-branch.sh` / knowledge 多層防御 hook はローカル PreToolUse hook であり、GitHub Actions 上の claude-code-action には非適用。**workflow レベルの宣言的制御が追加で必要**:

1. `.github/workflows/claude*.yml` の `permissions:` ブロックに最小スコープのみ列挙（workflow/job レベルで enforce）
2. `if: github.event.pull_request.head.repo.full_name == github.repository`（Fork PR 除外の二重宣言）
3. トリガーを `pull_request` に限定（`pull_request_target` 使用禁止）

### autonomous-restrictions.md 更新要否

**要追記**。現行定義はローカル自律実行ループのガードレールを前提としており、CI エージェントが定義の射程外。以下のカテゴリを追加:

```text
### 6. CI エージェント（GitHub Actions）
claude-code-action 等の CI 上 Claude エージェントに与える権限の変更。
- .github/workflows/claude*.yml の permissions / secrets 参照変更
- pull_request_target トリガーへの変更
- CLAUDE_CODE_OAUTH_TOKEN / ANTHROPIC_API_KEY の参照方式変更
```

### 攻撃チェーン

最大リスク経路: `pull_request_target` + `secrets` 参照の組み合わせ。Fork PR の悪意ある contributor がコメントにプロンプトインジェクションを埋め込み → claude-code-action が実行 → `CLAUDE_CODE_OAUTH_TOKEN` を外部送信。深刻度: High（設計ミス時）/ Low（正しい設計時）。

### 承認条件（5点）

1. GitHub App スコープを `contents: read / pull-requests: write / issues: write` に限定
2. トリガーを `pull_request` に限定（`pull_request_target` 禁止）
3. `.github/workflows/claude*.yml` に `permissions:` ブロックを明示宣言
4. `autonomous-restrictions.md` に「6. CI エージェント（GitHub Actions）」を追記
5. `CLAUDE_CODE_OAUTH_TOKEN` を GitHub Actions environment secrets（保護ルール付き）で管理

---


## 2026-04-29 — 不可領域チェック: docs/SECURITY.md・README.md の多層防御アーキテクチャ反映（Issue #439/#442/#448 docs 反映）

### 判定

OK（通過）— 5分類いずれにも非該当

### 対象ファイル

- `docs/SECURITY.md` — 監査ログパス更新（L214）、ガードレール記述追加
- `docs/specification.md` — harvest-all-active スタンプ説明補強
- `docs/migration-knowledge-buffer.md` — ヘッダー検知パターン追記
- `docs/ai-organization.md` — C*O 共通 tools セット統一リスト化
- `README.md` — ガードレール多層防御・buffer 統一フローへの言及追加

### 照合結果

| 分類 | 判定 | 根拠 |
|------|------|------|
| 1. 認証 | 非該当 | 認証フック・permissions セクションの変更なし |
| 2. 暗号 | 非該当 | encrypt/decrypt/secret/credential/token を扱うコードの変更なし |
| 3. 課金構造 | 非該当 | docs/cost-analysis.md・コスト上限・LLM 呼び出し箇所の変更なし |
| 4. ガードレール | 非該当 | protect-files.sh / diagnose-guard.sh 本体の変更なし。既存のガードレール（protect-knowledge-direct-writes.sh 等）をドキュメントに記述追加するのみ |
| 5. MVV | 非該当 | MVV.md の変更なし |

### 攻撃チェーン観点

全6修正項目が `docs/` または `README.md` へのドキュメント記述のみ。フックスクリプト・スキル・settings.json のコードは変更しない。新たな攻撃経路は生じない。

### 深刻度

低（ドキュメント整合性向上のみ）

### 過去判断との一貫性

- 2026-04-20「docs/SECURITY.md 誤記修正」: 同種のドキュメント整合性修正を承認済み。矛盾なし
- 2026-04-29「Issue #442 knowledge 構造統一」: buffer フロー境界維持で承認済みの設計の docs 反映であり、一貫


## 2026-04-29 — Issue #452 計画メタレビュー: protect-knowledge-bash-writes.sh 実装照合

### 判定

承認（計画の記述が実装と完全一致）

### 対象

- 計画ファイル: `~/.cache/vibecorp/plans/vibecorp-54064690/dev_452_sync_docs_multilayer_defense.md`
- 照合対象フック: `templates/claude/hooks/protect-knowledge-bash-writes.sh`
- 更新対象: `docs/SECURITY.md`（Phase 1a L214 パス更新 + Phase 1b 新セクション）

### 照合結果

#### Phase 1b: Bash 層検出パターン列挙の正確性

計画が列挙する 6 パターンとフック実装（L79-104）の対応:

| 計画の記述 | フック実装 | 一致 |
|-----------|-----------|------|
| `>` / `>>` リダイレクト | L81: `BASH_REMATCH` で `\>{1,2}` を捕捉 | 一致 |
| `tee` / `tee -a` | L87: `tee[[:space:]]+(-a[[:space:]]+)?` | 一致 |
| `cp` / `mv` | L93: `(cp\|mv)[[:space:]]+...` の宛先抽出 | 一致 |
| `sed -i` / `sed -i ''` | L99: `sed[[:space:]]+-i([[:space:]]+\'\')?` | 一致 |
| `awk -i inplace` | L99: `awk[[:space:]]+-i[[:space:]]+inplace` | 一致 |
| heredoc + redirect | `cat <<EOF > path` はリダイレクト `>` パターン（2a）で捕捉 | 一致（計画の記載通り） |

コマンド正規化（環境変数プレフィックス L38-40、`env`/`command` L44-61、`bash -c` 展開 L64-71）も計画記述と一致。

#### 既知ギャップの記述

計画「`$''` quote / shell function 経由 / `eval` 経由」= フックヘッダー L12-14 記述と完全一致。

#### fail-secure 原則

`_pkw_normalize_path` が失敗すると `abs_detected_path` が空文字列になり、L115 の 3 段ガード（`buffer_dir 非空 + abs_buffer_dir 非空 + prefix 一致`）が通過できず deny に至る。計画の「パス正規化失敗時は deny（fail-closed）」記述と実装が整合。

#### Phase 1a: L214 パス更新

現行 L214: `knowledge/security/audit-YYYY-MM-DD.md`
更新後: `security/audit-log/YYYY-QN.md`（四半期集約）+ `audit-log-index.md` 追記

Issue #442 で確立した buffer worktree 側の実構造（`/Users/hiroki/.cache/vibecorp/buffer-worktree/vibecorp-54064690/.claude/knowledge/`）と一致。過去エントリ「2026-04-29 Issue #442 承認」とも整合。

### 脅威評価

- docs のみの変更でコード変更なし
- 新たな攻撃経路なし
- 深刻度: 低

### 最終判断

承認: 計画の SECURITY.md 記述内容が実装と矛盾なし。Phase 1a/1b の実施可。


## 2026-04-20 — PR #382 CodeRabbit 指摘: docs/SECURITY.md プリセット別動作表 3者ゲート列の誤記修正

### 判定

承認（ドキュメント整合性修正のみ、コード変更なし）

### 対象

- `docs/SECURITY.md` — 「自律実行承認ゲート（Issue #361）」セクション内「プリセット別動作」表

### 問題（CodeRabbit Major 指摘）

standard・minimal 行の「3者ゲート」列が「N/A」となっていたが、同ファイル L178 の記述（「/issue スキル（full/standard プリセット）でのIssue起票時に、CISO・CPO・SMの3者が不可領域フィルタを実施する」）と矛盾していた。

- standard プリセット: /issue スキル自体は存在し、起票時に3者ゲートが動作する
- minimal プリセット: /issue/SKILL.md L147「preset が minimal は全てスキップ（3者フィルタなし）」のとおり3者ゲートが入らない

### 修正内容

| 変更対象 | 変更前 | 変更後 |
|---------|--------|--------|
| standard 行 3者ゲート | N/A | /issue で実施 |
| minimal 行 3者ゲート | N/A | スキップ（/issue のみ動作） |

安全性根拠列は「/autopilot が存在しないため自動実装経路なし」のまま維持。

### 過去判断との一貫性

2026-04-20 の「standard 行 /autopilot 誤記修正」エントリとの整合性確認済み。
2026-04-20「Issue #361 /issue と /autopilot の責務分離」で「minimal プリセットでの3者ゲート非動作と安全性根拠」の明記を求めた条件の一部をドキュメント整合という形で実現。矛盾なし。

### 攻撃チェーン観点

N/A 表記は閲覧者に「standard にはゲートが存在しない」という誤認を与えうるが、実際の攻撃経路ではなくドキュメント誤読のリスク。コード・スキル定義の変更なし。深刻度: 低（ドキュメント整合性の問題のみ）。

## 2026-04-20 — docs/SECURITY.md プリセット別動作表の誤記修正（standard 行 /autopilot）

### 判定

承認（ドキュメント整合性修正のみ、コード変更なし）

### 対象

- `docs/SECURITY.md` — 「自律実行承認ゲート（Issue #361）」セクション内「プリセット別動作」表

### 問題

standard 行の /autopilot を「動作する」としていたが、実態と乖離していた。

- `templates/claude/skills/autopilot/SKILL.md` 前提条件:「full プリセット専用」
- SKILL.md ステップ1: full 以外の場合は終了

standard プリセットには /autopilot スキル自体が配置されないため、「動作する」という記述はドキュメントとして誤りであり、セキュリティポリシーの誤認を招く。

### 修正内容

| 変更対象 | 変更前 | 変更後 |
|---------|--------|--------|
| standard 行 /autopilot | 動作する | 動作しない |
| standard 行 3者ゲート | /issue で実施済み | N/A |
| standard 行 安全性根拠 | 同上 | /autopilot が存在しないため自動実装経路なし |

### 攻撃チェーン観点

修正前の誤記は「standard でも /autopilot が動く」という誤った期待を生む可能性があった。実態として /autopilot が存在しないため自動実装経路は存在せず、攻撃チェーンとして成立しない。ドキュメント整合性の問題のみ。


## 2026-04-16 — Issue #309 macOS sandbox-exec PoC（Phase 1）

### 判定

REQUEST_CHANGES（必須修正 3 点の対応後に再レビュー）

### 対象

- `templates/claude/bin/claude`（PATH シム）
- `templates/claude/bin/vibecorp-sandbox`（OS ディスパッチャ）
- `templates/claude/sandbox/claude.sb`（sandbox-exec プロファイル）
- `tests/test_isolation_macos.sh`（テスト）

### 合議状況

security-analyst 3名全員一致で脆弱性検出。全会一致ルール適用。
CodeRabbit CLI 11件の指摘も同領域を網羅。

### 必須修正（Phase 1 ブロッカー）

1. **$WORKTREE/$HOME 未サニタイズ注入**（S-001/S-011/T-003）
   - .claude/rules/shell.md 明確違反（Minor）
   - 修正内容: vibecorp-sandbox で -D 引数に渡す前に空文字列・ルートパス（/、~）・パストラバーサル（../）を拒否するバリデーションを追加

2. **VIBECORP_SANDBOXED=1 外部注入バイパス**（S-005/S-009/T-001）
   - Phase 1 設計目的（最低限の境界）を自己否定する構造（Major）
   - 修正内容: PID 検証導入、または既知の制限としてコメント明記 + Phase 2 Issue 起票

3. **テスト[3]がバイパスを正常動作として検証**（T-006）
   - 修正 2 に連動。バイパスのネガティブテストを追加（Minor）

### Phase 2 以降スコープ出し（設計変更レベル・Info 相当）

- process-exec ホワイトリスト化（S-004/T-002）
- network* 全許可の絞り込み（S-003/T-004）。~/.config/gh 読取との組み合わせによる PAT 漏洩経路を Issue に明記すること
- ~/.claude 全 RW 許可の範囲見直し（S-010）
- /Library・/private/var/db 過剰読取の限定（S-007/S-013/T-005/T-008）
- mach-lookup/ipc-posix-shm/sysctl-read の精査（S-006/S-015/S-016）
- HOME 改ざんリスク・CI 環境対策（S-012）
- PROFILE の TOCTOU 対策（T-007）

### 却下した指摘（Info 相当）

- S-008: テスト mktemp プレフィックス予測可能性（攻撃面限定的）
- S-017: ~/.aws/.gnupg 読取拒否テスト不在（deny default が機能）
- T-005（Keychains 明示 deny 不在）: deny default が保護機能中
- S-015/S-016: PoC で許可を広めに取ることが設計計画書に明記済み

### 判断根拠

Phase 1 は opt-in かつテンプレート配置のみ（install.sh 未連携）であるため、
設計変更レベルの指摘は Phase 2 以降に委ねた。
ただし .claude/rules/shell.md の明確な規約違反と、
Phase 1 の設計目的そのものを崩す VIBECORP_SANDBOXED バイパス構造は
スコープ内で修正すべきと判断した。

### 攻撃チェーン分析

最大リスク経路: 
VIBECORP_SANDBOXED=1 外部注入 → サンドボックス完全スキップ
→ WORKTREE=$HOME 設定（未サニタイズ） → プロファイル境界崩壊
→ ホームディレクトリ全体への書込権限 + ~/.config/gh 読取
→ GitHub PAT の外部送信（network* 全許可）

この経路は Phase 3（install.sh 配布）以降で全ユーザーに波及するため、
Phase 1 で入口部分（サニタイズ・バイパス構造）を修正しておくことが重要。


## 2026-04-16 — CR-001 CodeRabbit 第 2 回レビュー対応（WORKTREE ⊇ HOME 攻撃経路）

### 判定

承認（Phase 1 防御強化として対処済み）

### 対象

- `templates/claude/bin/vibecorp-sandbox`（境界検証ロジック）
- `tests/test_isolation_macos.sh`（ネガティブテスト [8] 追加）

### 合議状況

CodeRabbit 第 2 回レビューで CR-001（Critical）として検出。
S-001/S-011/T-003（WORKTREE の未サニタイズ注入）の追加攻撃経路として位置づける。

### 攻撃チェーン分析

WORKTREE ⊇ HOME 注入（`WORKTREE=/Users` / `$HOME/subdir` / symlink 経由）
→ `(subpath (param "WORKTREE"))` が `~/.ssh`・`~/.aws`・`~/.gnupg` を RW 範囲に包含
→ sandbox 境界崩壊
→ SSH 鍵 / AWS クレデンシャル / GPG 鍵への書込

初回レビュー（S-001/S-011/T-003）での等値比較バリデーションでは、symlink 経由・包含関係の攻撃経路を検出できなかった。

### 修正内容と根拠

- `canonicalize_dir()` で symlink を解決し、raw バリデーション → canonicalize → canonicalize 後の再バリデーションという 2 段階検証を実装
- WORKTREE ⊇ HOME の包含チェックを `case` 文で実装し、包含が検出された場合は起動を拒否
- 設計変更レベルではなく境界検証ロジックの精度向上と判断。Phase 1 スコープ内で対処が妥当
- `test_isolation_macos.sh [8]` のネガティブテストにより攻撃チェーン封鎖の根拠を確認（13/13 passed）

### 過去判断との一貫性

初回判断（2026-04-16 Issue #309）の S-001/S-011/T-003「$WORKTREE/$HOME 未サニタイズ注入」の追加対応。矛盾なし。Phase 2 以降の制約（ネットワーク全許可、process-exec 無制限等）は引き続き Phase 2 で追跡予定。


## 2026-04-16 — Issue #318 Phase 3a: install.sh macOS 隔離レイヤ配置ロジック統合

### 判定

条件付き承認（Minor 2 点を Phase 3a スコープ内で修正、Low 以下はトラッキング済みとして却下）

### 対象

- `install.sh`（`copy_isolation_templates` / `generate_activate_script` / `check_isolation_deps` 追加）
- `tests/test_install_isolation.sh`（新規テスト）
- `tests/test_install.sh`（T テスト追加）
- `README.md` / `docs/SECURITY.md` / `docs/specification.md`（ドキュメント更新）

### 合議状況

- Analyst 1: Minor 1 件（symlink 追跡）、残りは Info
- Analyst 2: High 2 件（SCRIPT_DIR/REPO_ROOT symlink）、Medium 3 件（同上 + テスト 2 件）、Low 3 件
- Analyst 3: Minor 2 件（symlink 追跡 + cleanup cd `|| true` 欠落）

3 名中 1 名（Analyst 2）が High 判定 → 全会一致ルール発動。精査の上、High 判定の妥当性を検証した。

### High 判定の妥当性検証（P318-H-001 / P318-H-002）

**P318-H-001（SCRIPT_DIR の symlink 非解決）**

`SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"` は物理パスを解決しない（`pwd -P` 相当ではない）。

しかし攻撃前提が「git 管理下の `templates/` 配下ファイルが symlink にすり替えられる」ことであり、これはリポジトリそのものへの書込権限（または supply chain 侵害）を前提とする。vibecorp は git リポジトリを Source of Trust とする設計であり、リポジトリ改ざんは install.sh のスコープ外の前提侵害である。

また `copy_isolation_templates` は `[[ -f "$src" ]]` によりシンボリックリンクを素通しするため（P318-001）、symlink すり替えが成立しているとファイルがコピーされる。ただし SCRIPT_DIR が正しく解決されても symlink すり替えが発生すれば同じ問題であり、SCRIPT_DIR の `pwd -P` 化は「SCRIPT_DIR 自体が symlink」なケースへの対策にとどまる。Phase 1/2 で実装済みの `validate_abs_path` + `canonicalize_dir` は sandbox 実行経路（vibecorp-sandbox スクリプト内）に実装されており、install.sh の配置経路への多層防御は Phase 3a のスコープ外である。

**判定: High → Minor に格下げ**。`pwd -P` を使うことは望ましいが、攻撃の成立にはリポジトリ改ざんが前提であり、install.sh 単体でこの前提をくつがえすことは設計上困難。SECURITY.md の既知制約セクションへの追記と並行して、symlink 追跡（P318-001）の修正で対応が妥当。

**P318-H-002（generate_activate_script の REPO_ROOT symlink 非解決）**

activate.sh 内の `bin_abs="$(cd "$(dirname "$script_path")" && pwd)"` は source 実行時に物理パスを解決する。ここはインストール後のユーザー実行フェーズであり、install.sh の配置フェーズとは別問題。REPO_ROOT の symlink 解決は `detect_repo_root` 関数で行うべきだが、これは既存設計の課題でありPhase 3a 新規変更の直接的な脆弱性ではない。

**判定: High → Low（Phase 2 以降トラッキング）**。activate.sh の PATH 注入は `pwd` 経由で物理パスを取得しており、symlink 解決は呼び出し元シェルの PATH 展開の問題。設計変更が必要なレベルであり Phase 3a のスコープを超える。

### 採用する指摘

**Minor — Phase 3a で修正すべき**

| ID | 検出者 | 内容 | 採用根拠 |
|----|--------|------|----------|
| P318-001 | 全員一致（3/3） | `copy_isolation_templates` の `[[ -f "$src" ]]` がシンボリックリンクを通過する | 3名全員一致。`[[ ! -L "$src" ]]` の追加で排除可能 |
| P318-003 | Analyst 3 単独 | `cleanup()` の `cd "$SCRIPT_DIR"` に `\|\| true` なし。`.claude/rules/testing.md` 規約違反 | 規約明記の Trivial。単独指摘だが testing.md に明記された MUST 事項のため採用 |

### 却下する指摘

| ID | 検出者 | 却下理由 |
|----|--------|----------|
| P318-H-001 | Analyst 2 単独 | リポジトリ改ざんを前提とする攻撃経路。install.sh スコープ外の前提侵害。Minor に格下げして P318-001 修正で包含 |
| P318-H-002 | Analyst 2 単独 | activate.sh の PATH 解決はインストール後フェーズの設計課題。Phase 3a 新規変更の直接的脆弱性ではない。Phase 2 以降でトラッキング |
| P318-M-002 | Analyst 2 単独 | G テストの `$R` 設定順序。`create_test_repo` がグローバル変数 `TMPDIR_ROOT` を設定し、その後 `R="$TMPDIR_ROOT"` とすれば動作する。diff L702 確認: G テストで `create_test_repo` を先に呼び `R="$TMPDIR_ROOT"` は呼んでいない（`R` 未設定のままパスを構築）。これは Info 相当のテスト信頼性問題だが、G テストは `PATH="$FAKE_BIN" bash ...` 形式で参照しておらず実害なし。Trivial に格下げして Phase 2 以降でトラッキング |
| P318-M-003 | Analyst 2 単独 | F テストの `bash -c` が呼び出し元 PATH を継承。これはテストの意図通り（activate.sh を source した後の PATH 変化を検証するため既存 PATH を引き継ぐことが前提）。却下 |
| Info 各種 | 各アナリスト | README.md 永続化警告不足、generate_activate_script デッドコード、$R シングルクォート混入、command -v sandbox-exec PATH 依存、chmod +x 不要、cleanup cd の順序 → 情報提供のみ / 軽微スタイル |

### Phase 3a で修正すべき項目

1. **`install.sh` の `copy_isolation_templates`（L225 / L233 付近）**
   - `[[ -f "$src" ]]` を `[[ -f "$src" ]] && [[ ! -L "$src" ]]` に変更
   - シンボリックリンクのファイルを配置対象から除外する
2. **`tests/test_install_isolation.sh` の `cleanup()` 内（L565）**
   - `cd "$SCRIPT_DIR"` を `cd "$SCRIPT_DIR" || true` に変更
   - `.claude/rules/testing.md` 規約: cleanup 内のリソース解放コマンドは `|| true` で失敗を無害化

### Phase 2（#310）以降でトラッキングする項目

| 項目 | 理由 |
|------|------|
| SCRIPT_DIR の `pwd -P` 化（P318-H-001 格下げ版） | リポジトリ改ざん前提の経路。Phase 2 で多層防御追加時に見直し |
| REPO_ROOT の symlink 解決強化（P318-H-002） | `detect_repo_root` の設計変更が必要。Phase 2 以降のスコープ |
| G テストの `$R` 設定タイミング（P318-M-002） | テスト信頼性の改善。次回テストリファクタリング時に対処 |

### 攻撃チェーン分析

`install.sh` の `copy_isolation_templates` が symlink を通過するケース（P318-001）が最大リスクだが、攻撃の前提は「`templates/claude/bin/` 配下にシンボリックリンクが混入していること」であり、これはリポジトリへの書込権限を前提とする。sandbox 実行経路（vibecorp-sandbox）は Phase 1/2 で `canonicalize_dir` + 包含チェックの 2 段階検証が実装済みであるため、install.sh の配置フェーズを通過しても sandbox 境界崩壊には至らない。

深刻度: **低**（Phase 1/2 の防御レイヤが配置後の実行時を保護）

### 過去判断との一貫性

- #309 判断「Phase 3（install.sh 配布）以降で全ユーザーに波及するため、Phase 1 で入口部分（サニタイズ・バイパス構造）を修正しておくことが重要」に合致。Phase 3a として最低限の symlink 排除（P318-001）を適用する。
- CR-001（WORKTREE ⊇ HOME 攻撃経路）の 2 段階検証は install.sh 配置後の sandbox 実行時に有効化されるため、Phase 3a の修正と干渉しない。


## 2026-04-16 — Issue #320 Phase 1 sandbox 境界拡張（TUI ハング修正）

### 判定

条件付き承認（Phase D 実装時に 2 点の修正を反映後、Phase E-1 最終確認で承認）

### 対象

- `templates/claude/sandbox/claude.sb`（sandbox 境界拡張）
- `install.sh`（`setup_claude_real_symlink` 追加）
- `docs/SECURITY.md`（境界・制約表の更新）

### 合議状況

計画段階の事前 CISO レビュー。平社員層レビュー（単一モード・5件指摘）の妥当性評価を兼ねる。

### 各境界拡張の評価

**A: `~/.local/share/claude/**` RO 許可 — 条件付 OK**

- バイナリ実体への RO のみ。書き換え不可。
- 情報漏洩リスクは既存の `~/.npm` RO 許可と同等かそれ以下。
- SECURITY.md 更新案の `.claude.json.lock` 残存（D-1 指摘）のみ要修正。

**B: `~/.claude.json` / `.backup` RW 許可 — 条件付 OK**

- `~/.claude` 全 RW 許可と同等の信頼境界。攻撃面は実質変わらない。
- `(literal ...)` による単一ファイル限定は最小権限の観点で妥当。
- SECURITY.md の既知制約表にこの評価根拠を一文追記することが条件（D-2 指摘）。

**C: `/dev/**` file-ioctl 全許可 — 条件付 OK**

- `/dev` への read/write-data は既に許可済み。ioctl 追加で新たな攻撃面は限定的。
- `/dev/mem` は現代 macOS で無効化済み。IOKit との組み合わせは Phase 1 既知制約の範囲内。
- 個別 ioctl 番号絞り込みは Phase 2 以降トラッキング。Phase 1 PoC として全許可は妥当。
- SECURITY.md 既知制約表への追記が必要。

### 平社員指摘の評価（全5件採用）

1. `.claude.json.lock` 境界削除 — 採用。ただし SECURITY.md 更新案から `.lock` が除去されていない（D-1 指摘として別途修正が必要）。
2. `-x` 単独検出条件 — 採用。セキュリティ観点で問題なし。
3. minimal/standard 削除ブロック 2 箇所明記 — 採用。不完全な symlink 残存を防ぐために必須。
4. `tests/test_install_claude_real.sh` 新規追加 — 採用。testing.md 規約必須。
5. Phase B-3 検証を Phase B-4 テストでカバー — 採用。フェーズ対応の明確化として妥当。

### 新規懸念

- **D-1（SECURITY.md `.lock` 矛盾）:** 計画書の SECURITY.md 更新案（p.215 表）に `.claude.json.lock` が残存。Phase D-1 実装時に削除すること。
- **D-2（B の評価根拠追記）:** `~/.claude.json` RW の既知制約説明に「`~/.claude` 全 RW と同等の信頼境界であり攻撃面は変わらない」旨を追記すること。
- **D-3（`claude-real` symlink サプライチェーン）:** PATH 汚染環境での悪意バイナリ誤 symlink は Phase 1 許容範囲内。SECURITY.md の既知制約に「`claude-real` symlink 先の検証なし（PATH 汚染環境は対象外）」を追記推奨。

### 攻撃チェーン分析

今回の 3 境界拡張はいずれも Phase 1 既知制約（ネットワーク全許可・`~/.claude` 全 RW・process-exec 無制限）の範囲内にとどまる。CR-001 で封鎖した WORKTREE 境界・HOME 包含拒否の防御ロジックは `vibecorp-sandbox` スクリプト側に実装されており、`claude.sb` 境界拡張に影響されない。深刻度: 低。

### 過去判断との一貫性

- #309 および CR-001 判断と矛盾なし。Phase 1 スコープ内の境界拡張として整合している。
- #318 の「Phase 3a 以降で全ユーザーに波及する」前提のもと、SECURITY.md へのドキュメント整合（D-1/D-2）が重要。

### 最終評価（実装後）— 2026-04-16 Phase E-1

#### 判定

承認: セキュリティリスクなし

#### D-1/D-2/D-3 反映確認

- **D-1（`.lock` 除去）:** `claude.sb` に `.claude.json.lock` のエントリなし。SECURITY.md の書込境界表にも記載なし。反映済み。
- **D-2（評価根拠追記）:** SECURITY.md の既知制約表の `~/.claude.json` 全 RW 許可行に「評価根拠: 既存の `~/.claude` 全 RW 許可と同等の信頼境界であり、攻撃面は実質変わらない（CISO メタレビュー 2026-04-16）」が明記されている。反映済み。
- **D-3（symlink 検証なし）:** SECURITY.md の既知制約表に「`claude-real` symlink 先の検証なし」行として PATH 汚染環境はスコープ外である旨が追記されている。反映済み。

#### 境界実装の整合性

`claude.sb` を直接検証した。3 境界拡張（`~/.local/share/claude` RO / `~/.claude.json` `.backup` RW literal / `/dev` file-ioctl）はすべて計画通りに実装されており、計画段階で承認した範囲を超える権限追加は存在しない。

#### テスト [9][10] の評価

- テスト [9]: 実 HOME を使って sandbox 経由で `claude --version` を実行。`~/.local/share/claude` RO 許可と `~/.claude.json` RW 許可が破綻した場合に検出できる構造。
- テスト [10]: `expect` で TUI を起動し ANSI エスケープ（raw mode 証跡）を検出。`/dev` file-ioctl 許可が破綻した場合に検出できる構造。
- 両テストとも CI では実機 claude 不在により skip される設計で、SECURITY.md に明記済み。CI 偽陽性を生まない。

#### CR-001 観点の再評価メモの一貫性

SECURITY.md の再評価メモは攻撃チェーン分析と整合している。特に「CR-001 で封鎖した WORKTREE 境界・HOME 包含拒否の防御ロジックは `vibecorp-sandbox` スクリプト側に実装されており、`claude.sb` 境界拡張に影響されない」という記述は、境界拡張が防御ロジックを迂回しないことを明示しており妥当。

#### 攻撃チェーン分析

今回の実装によって新たな攻撃経路が生まれていないことを確認した。3 境界拡張はいずれも Phase 1 既知制約（ネットワーク全許可・`~/.claude` 全 RW・process-exec 無制限）の範囲内にとどまる。深刻度: 低。

#### 過去判断との一貫性

計画段階（条件付き承認）の条件がすべて充足された。矛盾なし。PR 作成に進んでよい。


## 2026-04-16 — Issue #320 合議制最終チェック（security-analyst ×3 メタレビュー）

### 判定

承認: セキュリティリスクなし

### 対象

コミット 8bef923（隔離レイヤ Phase 1 — claude TUI ハング修正）の差分全体。

### 合議状況

security-analyst 3名全員一致で「問題なし」。全会一致ルール適用なし（Major 以上の検出ゼロ）。

### 共通指摘の最終評価

**P320-readlink（install.sh L230 付近）**

`readlink claude` が相対パスを返した場合にラッパー除外パターン不一致が生じる。ただし最悪ケースは exec ループ（DoS 相当）に限定され、sandbox 境界崩壊・権限昇格・情報漏洩には至らない。SECURITY.md 既知制約「`claude-real` symlink 先の検証なし（PATH 汚染環境は対象外）」および decisions.md D-3 判断でスコープ外と位置付け済み。Minor 評価を支持。

**P320-expect-vars（tests/test_isolation_macos.sh L673-676）**

`expect -c` のインライン変数展開でスペース含むパスが引数分割される可能性。テストコードのみに影響し、プロダクションの sandbox 境界・シム・vibecorp-sandbox スクリプトには影響しない。Minor 評価を支持。

### 攻撃チェーン分析

2 指摘を連鎖させた攻撃経路を検討したが、sandbox 境界崩壊・権限昇格に至らないことを確認した。WORKTREE 境界・HOME 包含拒否の防御ロジック（vibecorp-sandbox 側）は今回の変更ファイルで改変されていない。深刻度: 低。

### 過去判断との一貫性

Phase E-1「承認: セキュリティリスクなし。PR 作成に進んでよい」および D-3「PATH 汚染環境はスコープ外」と矛盾なし。


## 2026-04-17 — Issue #326 ゲートスタンプの XDG キャッシュ移動（docs/SECURITY.md 不整合修正）

### 判定

承認: セキュリティリスクなし（ドキュメント不整合の修正のみ）

### 対象

- `docs/SECURITY.md`（書込・読取境界表の更新 + #326 説明ノート追記）

### 合議状況

ドキュメント不整合修正のため security-analyst 合議なし。CISO 単独レビュー。

### メタレビュー内容

Issue #326 / PR #327 にてゲートスタンプの保存先が `.claude/state/` から `${XDG_CACHE_HOME:-$HOME/.cache}/vibecorp/state/<repo-id>/` に移動済み。sandbox プロファイル（`claude.sb`）に `~/.cache/vibecorp` の subpath 許可も追加済み。しかし `docs/SECURITY.md` の書込・読取境界表にこの変更が未反映だった（sync-check 検出）。

### 変更内容

1. **書込境界表の「書込」行に追記**: `~/.cache/vibecorp`（ゲートスタンプ保存先 #326）
2. **Issue #326 説明ノート追加（#320 パターンに倣う）**: 保存パスの形式・サブパス限定許可の根拠・脅威モデルを記載

### 攻撃チェーン分析

- `~/.cache/vibecorp` は `~/.cache` 全体ではなくサブパスのみを許可（攻撃面最小化。既に実装済み）
- 脅威モデル: 同一ユーザーの別プロセスからのスタンプ偽造はスコープ外（信頼境界 = ユーザーアカウント）
- 他ユーザーからの偽造は chmod 700 でブロック
- 新たな攻撃経路は生じない。深刻度: 低（既実装の境界をドキュメントに反映したのみ）

### 過去判断との一貫性

#320 の「`~/.cache` 全体ではなくサブパス限定」という攻撃面最小化の設計思想と一致。矛盾なし。


## 2026-04-17 — Issue #329 VIBECORP_ISOLATION=1 下の /login 失敗修正（サイドカー書込許可追加）

### 判定

条件付き承認（以下 4 点の対応後に承認）

### 対象

- `.claude/sandbox/claude.sb`（`~/.claude.json.lock` literal 許可 + `~/.claude.json.tmp.<pid>.<epoch_ms>` regex 許可追加）
- `templates/claude/sandbox/claude.sb`（同上、同期）
- `tests/test_isolation_macos.sh`（テスト [11][12] 追加）
- `docs/SECURITY.md`（書込境界表更新）

### 合議状況

計画段階レビュー（plan-security が Critical 1 件・Major 1 件を検出）。CISO メタレビューにより Critical/Major の再評価を実施。

### 各指摘の判定

| 指摘元 | 内容 | 判定 | 理由 |
|--------|------|------|------|
| plan-security Critical | CEO 承認トレーサビリティ欠落 | 却下（Info） | セキュリティ脆弱性ではなく監査ガバナンスの好みの問題。計画書に「CEO 承認済み」の記載あり |
| plan-security Major | HOME `.` がSBPL regex でメタ文字誤マッチ | 却下（Info） | 計画書 C1 節で既評価済み。`.` が誤マッチしても許可範囲は `.claude.json.lock/.tmp.*` のみ。`validate_abs_path` が危険メタ文字を事前排除済み |
| plan-security Minor | テスト [12] 境界値パターン不足 | 部分採用 | `.tmp.1.2.extra` / `.tmp.abc.1` の 2 パターン追加を推奨（実装ブロックなし） |
| plan-architect | `date +%s` 秒精度と epoch_ms 不一致 | 採用（コメント修正のみ） | regex は桁数不問のため動作は正しい。`.claude/rules/comments.md` 準拠でコメント修正 |
| plan-architect | `diff` 同期チェック未組込み懸念 | 却下（Info） | 計画書 Phase 1 に既記載 |
| plan-architect | `/login` 成功が CI 外 | 却下（Info） | Phase 1 からの既知設計。SECURITY.md に明記済み |
| plan-testing | テスト [12] パターン不足（重複） | 部分採用（上記と同一） | 同上 |
| plan-testing | `.claude/sandbox` 側テスト欠落 | 却下（Info） | `diff 0` 検証で同一性担保済み |
| plan-testing | `.lock` 既存状態ケース不在 | 採用（Minor） | 次回改善候補。実装ブロックなし |
| plan-dx 全件 | UX/CI 手順の懸念 | 却下（Info） | セキュリティリスクに非該当 |

### 新規懸念

**N-001（過去判断との一貫性違反 — 必須対応）**

#320 CISO 判断「D-1: `claude.sb` に `.claude.json.lock` のエントリなし。反映済み」を今回の変更が上書きする。kernel ログにより `.lock` deny が確認されたため再追加する必要性は正当だが、decisions.md・SECURITY.md に「#329 で再追加した背景（#320 D-1 の方針変更）」を明記すること。

**N-002（sandbox param 未設定時の regex 動作確認 — Minor）**

`(regex (string-append (param "HOME") "..."))` は sandbox-exec 起動時に評価される。テスト [11] フェイク環境で `-D HOME=...` が確実に渡されることを確認すること。

### #320 D-1 判断の上書き記録

**旧判断（2026-04-16）**: `claude.sb` に `.claude.json.lock` のエントリなし。D-1 により除去・反映済み。

**新判断（2026-04-17）**: kernel ログ `deny(1) file-write-create /Users/hiroki/.claude.json.lock` により、`/login` 実行時に `.lock` の書込が必須であることが実機確認された。`.lock` 除去判断を撤回し、literal 許可として再追加する。攻撃面への影響: `~/.claude.json` 全 RW 許可（#320 承認済み）と同等の信頼境界のため、実質的な攻撃面変化なし。

### 攻撃チェーン分析

- 追加される regex: `^<HOME>/\.claude\.json\.tmp\.[0-9]+\.[0-9]+$`（`^`/`$` 固定）
- `vibecorp-sandbox` の `validate_abs_path` により HOME の危険文字を事前排除済み
- regex インジェクションで追加の書込権限を得る経路なし
- `.lock` literal 許可: 単一ファイルのみ
- 深刻度: 低（Phase 1 既知制約の範囲内）

### 過去判断との一貫性

#320 D-1 を明示的に上書き（理由: kernel log による必要性の確認）。それ以外の #309/CR-001/#318/#320/#326 の判断とは矛盾なし。


## 2026-04-17 — Issue #331 VIBECORP_ISOLATION=1 下の /login 失敗修正 Part2（XDG サイドカー拡張）

### 判定

承認（Phase 1 既知制約の範囲内、CR-001 観点で新規攻撃面なし）

### 対象

- `.claude/sandbox/claude.sb`（`~/.cache/claude` / `~/.local/state/claude` subpath RW 許可追加）
- `templates/claude/sandbox/claude.sb`（同上、同期）
- `tests/test_isolation_macos.sh`（テスト `[15][16][17]` 追加）
- `docs/SECURITY.md`（書込境界表更新、#331 セクション追加）

### 背景

Issue #329 マージ後も `VIBECORP_ISOLATION=1 claude /login` が失敗。kernel deny ログにより Claude Code 2.1.112+ が XDG 3 ディレクトリにまたがって OAuth state を管理することが判明:

- `~/.local/share/claude/versions/<ver>/` — バイナリ実体（RO で既許可、#320）
- `~/.local/state/claude/locks/<ver>.lock` — バージョン固有ロック（書込必須、未許可）
- `~/.cache/claude/staging/` — staging 領域（書込必須、未許可）

### 許可戦略の比較と選択

| 案 | 許可範囲 | 評価 |
|----|---------|------|
| A | `~/.cache/claude` + `~/.local/state/claude` subpath RW | **採用**。バージョン変更に強く、攻撃面は既存 `~/.claude` 全 RW と同等 |
| B | `~/.local/state/claude/locks/<ver>.lock` を literal で個別指定 | 却下。バージョン追従のたびに sandbox 修正が必要で運用負荷大 |
| C | `~/.cache` / `~/.local/state` 全体 | 却下。他アプリのサイドカーまで巻き込み攻撃面拡大 |

### 攻撃チェーン分析

- 追加される subpath: `~/.cache/claude` + `~/.local/state/claude`
- `claude-evil` 等の兄弟ディレクトリは subpath 境界で拒否（テスト [17] で検証）
- `vibecorp-sandbox` の `canonicalize_dir` により HOME は symlink 解決後の絶対パス
- 攻撃面: 既存の `~/.claude` 全 RW 許可（#309 承認済み）と同等の信頼境界
- 深刻度: 低（Phase 1 既知制約の範囲内）

### 過去判断との一貫性

Issue #320 / #326 / #329 と整合。`~/.cache/vibecorp` サブパス限定（#326）と同じ設計思想で、`~/.cache` 全体ではなく `claude` 直下のみに限定することで攻撃面最小化を維持。


## 2026-04-18 — プリセット別安全性評価（full/standard/minimal × sandbox/Hook）

### 判定

条件付き承認（full のみ）/ standard・minimal は Hook 削減不可ラインを明示

### 対象

CEO 提示の設計想定: full = sandbox-exec + --dangerously-skip-permissions + auto

### メタレビュー判定

- 「sandbox があれば Hook 不要」という単純化: **反対**。代替不可な Hook が存在する
- skip-permissions の安全条件: sandbox 内 + 明示的 opt-in の両方が必須

### 攻撃チェーン分析

**sandbox で防げない脅威（Phase 1 既知制約より）:**
1. ネットワーク全許可 → `~/.config/gh` 読取と組み合わせた GitHub PAT 外部送信
2. process-exec 無制限 → osascript 等による sandbox 境界迂回
3. `~/.claude` 全 RW → hooks 書き換えによる持続的挙動改変
4. git コマンド自体はワークツリー内操作として許可される（sandbox は git を止めない）

**標準 Hook 群の評価（sandbox で代替不可かどうか）:**
- `protect-files.sh`: MVV.md 等の保護ファイルへの書込。sandbox は書込境界を制御するが「特定ファイル禁止」の粒度はない。**代替不可**
- `diagnose-guard.sh`: 自律ループ暴走防止。sandbox は対象外。**代替不可**
- `block-api-bypass.sh`: gh api 直接マージ。git/gh はワークツリー内として許可される。**代替不可**
- `role-gate.sh`: エージェントロール越権防止。sandbox は対象外。**代替不可**
- `review-gate.sh` / `sync-gate.sh` 等: プロセスゲート。sandbox で防げない論理的制御。**代替不可**

### 最終判断

- full: 条件付き承認（sandbox + opt-in 両方が揃っている場合のみ skip-permissions 可。Hook は削減不可）
- standard/minimal: sandbox なし前提。protect-files / diagnose-guard / block-api-bypass は削除不可最低ライン

### 過去判断との一貫性

#309 の「ネットワーク全許可・process-exec 無制限は Phase 2 以降」制約と整合。Phase 1 既知制約の範囲で Hook が補完層として機能することを明示。


## 2026-04-18 — Issue #296 protect-branch.sh worktree 誤検知修正

### 判定

承認: セキュリティリスクなし

### 対象

- `.claude/hooks/protect-branch.sh`（ALLOWED_ROOT + realpath による新規パストラバーサル防御追加、worktree 内 Edit/Write 誤検知修正）
- `templates/claude/hooks/protect-branch.sh`（同期）
- `tests/test_protect_branch.sh`（WT-1〜WT-11 + DIFF-1 追加）
- `docs/known-limitations.md`（Bash 経路の既知制限を明記）

### 合議状況

security-analyst 3名の合議結果（audit-log.md: P296-001〜P296-004）。
Critical / High / Medium の検出ゼロ。全会一致ルール適用なし。

| ID | 重要度 | 合議判定 |
|----|--------|---------|
| P296-001 | Minor | 全員一致で検出。既存問題・別 Issue 後続化方針で一致 |
| P296-002 | Minor | 全員一致で検出。jq --arg による JSON インジェクション回避済み、攻撃面限定的 |
| P296-003 | Info | 対応不要（deny を受けるのは Claude Code エージェント自身であり外部公開されない） |
| P296-004 | Info | 対応不要（運用規約依存の設計上のトレードオフ） |

### メタレビュー判定

**P296-001（sed quote-aware 違反）の扱い**

`shell.md` に明示されている禁止事項違反（Minor）だが、実装計画（懸念事項6）および `protect-branch.sh` L157 の TODO コメントで「既存問題・本 Issue 範囲外」として認識・記録済み。別 Issue での後続化が確定しており、本 Issue の承認をブロックする理由はない。

**新規パストラバーサル防御の評価**

`ALLOWED_ROOT = CLAUDE_PROJECT_DIR/..` 配下のみ許可、`realpath` による symlink 解決、パス深さ10制限の組み合わせにより、Edit/Write 経路でのパストラバーサルが封鎖されている。テスト WT-4〜WT-8 でバイパス困難であることを確認。

**攻撃チェーン俯瞰**

P296-001（Bash 経路のセグメント分割バイパス）と P296-002（base_branch 汚染による deny 条件バイパス）を連鎖させた攻撃経路を検討したが、Bash 経路は `docs/known-limitations.md` に既知制限として明記済みであり、P296-002 の攻撃前提（vibecorp.yml への悪意ある記述）はリポジトリ管理者権限を要するため実際の攻撃面は限定的。Critical / High に相当する攻撃経路は存在しない。

### 攻撃チェーン分析

- Edit/Write 経路: ALLOWED_ROOT + realpath の 2 段階で封鎖。バイパス困難。
- Bash 経路: quote-aware でない sed によるコマンド分割が残存するが、known-limitations.md に明記済み。別 Issue で後続化予定。
- シンボリックリンク経由パス偽装: realpath が解決済み（CR-001 の canonicalize_dir 思想と同一）。
- 深刻度: 低

### 過去判断との一貫性

- CR-001 / #318 の「`canonicalize_dir` / realpath による symlink 解決」という防御思想と一致。矛盾なし。
- P296-001（sed quote-aware）は `shell.md` 規約の既知違反として別 Issue に委ねる。#309 / CR-001 以来の「既知制限を docs に明記して後続 Issue でトラッキング」方針に従った対応で整合している。

### 関連

- audit-log.md: 2026-04-18 Issue #296 セクション（P296-001〜P296-004）
- CR-001 decisions.md エントリ（canonicalize_dir 防御思想の起源）
- `docs/known-limitations.md`（Bash 経路の既知制限の明記先）


## 2026-04-19 — Issue #366 配布物の Source of Truth テンプレート化

### 判定

条件付き承認（awk セクション終端判定の Minor を今回スコープ内で修正推奨、P318-001 symlink チェックは別 Issue トラッキング継続）

### 対象

- `install.sh`（`migrate_tracked_artifacts` / `copy_claude_gitignore` 追加、`generate_activate_script` 削除・templates 移管）
- `templates/claude/.gitignore.tpl`（新規）
- `templates/claude/bin/activate.sh`（新規）
- `tests/test_install.sh`（AJ1〜AJ12・AK1〜AK5 追加）
- `docs/design-philosophy.md`（配布物 Source of Truth 原則セクション追加）
- `README.md`（tracked artifact 自動 untrack ドキュメント追加）

### 合議状況

security-analyst 3名全員が Critical / Major なし（全会一致ルール適用なし）。

| アナリスト | 判定 | 最高重要度 |
|-----------|------|----------|
| #1 | 問題なし | Minor（awk セクション終端・P318-001 継続） |
| #2 | Minor 2件・Info 3件 | Minor（`..` glob マッチ誤検知 / grep コメント行混入） |
| #3 | Minor 2件・Info 3件 | Minor（awk セクション終端 / P318-001 継続） |

### メタレビュー判定

#### 採用する指摘（今回スコープ内で修正推奨）

**P366-awk-terminator (Minor) — #1・#3 全員一致指摘**

`migrate_tracked_artifacts` 内の awk ロジック（`in_section` フラグ）に終端条件が未定義。現状の `.gitignore.tpl` は `machine-specific artifacts` セクションが末尾にあるため実害はないが、将来別セクションが追加された場合に後続エントリが `git rm --cached` 対象に混入する設計上の欠陥。

推奨修正:
```bash
in_section && /^# ----/ { in_section = 0; next }
```

`review-criteria.md` に基づき Minor は修正対象。2名一致指摘かつ設計上の潜在欠陥のため、今回スコープ内での修正が妥当。

#### 却下する指摘

**P366-2-001 (`..` チェックの誤検知リスク) — #2 単独指摘**

`[[ "$artifact" == *..* ]]` は `valid-..name/` のような正当パスも誤拒否しうる、という指摘。

却下理由: 入力元が vibecorp 管理下の `.gitignore.tpl` であり、ユーザー入力がそのまま流れる経路ではない。また `.claude/` プレフィックス強制が主防御として機能している。`%2e%2e` のすり抜けはシェルスクリプトのファイルシステム操作には影響しない（URL エンコードはシェルで展開されない）。`..` チェックは「深層防御の補助」として存在しており、誤検知リスクが現実には限定的。#1・#3 はこの問題を指摘していない。Info 相当として却下。

**P366-2-002 (grep コメント行混入) — #2 単独指摘**

`grep -vxF -f "$src" "$dest"` でテンプレートのコメント行が `existing_custom_lines` に混入する、という指摘。

却下理由: gitignore の動作上コメント行は無害。仮に重複コメントが追記されても機能上影響なく、ユーザー体験の軽微な劣化に留まる。Trivial 未満（Info）として却下。

#### 別 Issue トラッキング継続

**P318-001 symlink チェック欠如 — #1・#2 共通継続課題**

`copy_isolation_templates` の `[[ -f "$src" ]]` がシンボリックリンクを通過する問題（#318 CISO 判断で `[[ ! -L "$src" ]]` 追加を推奨済み）。今回の #366 変更で悪化はないが、未修正のまま継続。別 Issue でトラッキングを維持する。

### 攻撃チェーン分析

**最大リスク経路（awk 終端判定なし）:**
`.gitignore.tpl` に新セクションが将来追加された際 → `in_section` が終端されず後続の正当エントリまで抽出 → `git rm --cached` で意図しないファイルが untrack → ユーザーが次の commit で機密ファイルを意図せず公開

現状は実害ゼロ（テンプレートの現行構造で終端問題が発生しない）。ただし設計の脆弱さを今後の変更から保護するための修正が推奨される。深刻度: 低（現在）/ 中（テンプレート構造変更後の潜在的リスク）

**`migrate_tracked_artifacts` 全体の攻撃面:**
- 入力源: vibecorp 管理の `.gitignore.tpl`（ユーザー制御外）
- `.claude/` プレフィックス強制 + `..` チェックの 2 段階でパストラバーサルをブロック
- `git rm --cached` は index の untrack のみ（working tree に影響なし）
- 攻撃者がテンプレートを改ざんするには vibecorp リポジトリへの書込権限が必要（#318 分析と同等のスコープ外前提）

深刻度: **低**

### PR コメント要否

不要 — Minor 指摘（awk 終端判定）は PR の実装差分に対して明確に指摘可能だが、CISO メタレビューとして diff 上の該当箇所を特定した上で修正を依頼する形で十分。PR コメント投稿（`[CISO-consensus-review]` マーカー付き）は今回の指摘規模（Minor 1件）では過剰。

### 過去判断との一貫性

- #318 「`copy_isolation_templates` の symlink チェック欠如（P318-001）を Phase 3a で修正すべき」→ 未修正継続。今回の #366 では悪化なし。別 Issue での後続化方針（#318 decisions.md 記録）に変更なし。
- #309 「Phase 3（install.sh 配布）以降で全ユーザーに波及」前提: #366 は install.sh の新規機能追加であり、この観点での主要リスク経路（`.gitignore.tpl` 経由の `git rm --cached`）に 2 段階のバリデーションが実装されており整合している。


## 2026-04-20 — Issue #361 /issue と /autopilot の責務分離 + 3者承認ゲート導入

### 判定

条件付き承認（SECURITY.md への3者ゲート設計の明記、および /diagnose の3者ゲート実装確認を条件とする）

### 対象

- `templates/claude/skills/autopilot/SKILL.md`（ラベル縛り撤廃・起票側信頼の透過パイプ化）
- `templates/claude/skills/issue/SKILL.md`（CISO + CPO + SM の3者承認ゲート追加）
- `tests/test_autopilot.sh`（テスト6追加）
- `tests/test_autopilot_approval_gate.sh`（テスト7b追加・ISSUE_FILE 追加）
- `tests/test_issue.sh`（3者承認ゲート対応）

### 合議状況

スキル定義ファイルの構造変更（実行時コード・sandbox 境界の変更なし）のため security-analyst 合議なし。CISO 単独メタレビュー。

### メタレビュー判定

- 脆弱性: なし（既存設計の信頼境界内）
- 攻撃チェーン: 経路4（/diagnose の3者ゲート未実施 → 不可領域 Issue が autopilot で自動実装）が最大リスク。/autopilot が「diagnose 起票分は信頼できる」という前提の根拠として /diagnose の3者ゲート実装確認が必要
- SECURITY.md 整合: 要更新（3者承認ゲートの設計が未記載）
- 見落とし: プロンプトインジェクションリスク（Issue 本文による判定偽装）が既知制約として未明記
- 過去判断との一貫性: 一貫

### 設計評価

**起票側集約アーキテクチャの評価**

不可領域フィルタを起票側（/issue / /diagnose）に集約し、/autopilot を透過パイプとする設計は、全会一致ルール（1者でも除外なら起票中止）と整合している。

**minimal プリセットの安全性**

diff 内の説明は正確。minimal では /autopilot が動作しないため、不可領域 Issue の自動実装経路は存在しない。

**3者合議の全員 OK 要件**

「いずれかが除外 → 起票中止」の実装は全会一致ルールと整合。問題なし。

### SECURITY.md への必要追記

1. 3者承認ゲート（CISO + CPO + SM）の設計と起票側集約の原則
2. /autopilot が起票済み Issue を信頼する設計前提（/diagnose が3者ゲートを通過している必要性）
3. minimal プリセットでの3者ゲート非動作と安全性根拠
4. プロンプトインジェクション（Issue 本文による判定偽装）を既知制約として明記

### 攻撃チェーン分析

- 最大リスク経路: /diagnose が3者ゲートなしで不可領域 Issue を起票 → /autopilot が「起票済みなので信頼」して自動実装 → ガードレール・認証・暗号の自動変更
- プロンプトインジェクション経路: Issue 本文に「判定: OK」を含めて CISO/CPO/SM の返答を偽装。ただし CEO = 信頼境界の設計上、攻撃前提は CEO アカウントの侵害に限定される
- 深刻度: 低（/diagnose が autonomous-restrictions.md を参照してフィルタしている前提が保たれる限り）

### 過去判断との一貫性

- プリセット別安全性評価（2026-04-18）の「protect-files / diagnose-guard / block-api-bypass は削除不可最低ライン」と整合。本変更はこれらのガードレール Hook を変更しない。
- 全会一致ルール（#296 等で継続適用）に整合した「いずれかが除外 → 起票中止」の設計。矛盾なし。

## 2026-04-22 — 不可領域チェック: 承認ダイアログのスマホ remote app 非表示問題

### 判定

除外（ガードレール領域） — UI 伝達層修正アプローチ（アプローチ A）に対する判定

### 対象 Issue

- Issue: #392（`teammate が .claude/rules/ を編集する際の承認ダイアログが表示されなくなった`）
- タイトル: 「teammate の承認ダイアログがスマホ remote app に表示されない — ターミナル UI 描画のみで会話データに含まれない」
- 変更概要: `.claude/` 配下編集時の承認ダイアログが PC ターミナルには表示されるがスマホ remote app には表示されない。原因は jsonl 会話データに非記録。

### 評価対象アプローチの整理

本問題には 2 つの解決アプローチがある:

- **アプローチ A（UI 伝達層修正）**: 承認ダイアログを jsonl に記録し、スマホ remote app に表示・承認可能にする。`protect-files.sh` 等フックの承認要求ロジックに変更が及ぶ可能性がある
- **アプローチ B（allow リスト追加）**: `.claude/rules/**` を `permissions.allow` に追加し、そもそも承認ダイアログを発生させない。`protect-files.sh` の保護対象（`settings.json`・`CLAUDE.md`・`hooks/` 等）に `rules/` は含まれないため競合しない

本エントリはアプローチ A を評価対象として除外判定を行ったもの。アプローチ B は CTO が別途評価し承認している（2026-04-23 CTO 判断: Issue #392 参照）。PR #394 の実装はアプローチ B を採用した。

### 判断根拠

アプローチ A（承認ダイアログの「表示・伝達方式」の修正）は、`protect-files.sh` を含む承認フロー全体の動作変更を伴う可能性がある。`.claude/rules/autonomous-restrictions.md` が明示するガードレール不可領域に接触するリスクがあり、自律実行の対象から除外する。

不可領域の該当分類: **4. ガードレール**（`protect-files.sh` および承認フロー制御ロジック）

アプローチ B（`.claude/rules/**` の allow 追加）は `protect-files.sh` の保護対象に `rules/` が含まれないため、ガードレール不可領域に接触しない。CTO 判断により技術的非競合が確認されており、本 CISO 判定の除外対象外である。

### 攻撃チェーン分析

- 修正経路リスク（アプローチ A）: 「ダイアログを jsonl に記録する」修正として `protect-files.sh` 等フックの承認要求ロジックを変更した場合、承認フローのバイパス経路が生じうる
- 現状のリスク: スマホから承認ダイアログが表示されない状態は既に問題（承認なしで処理が通過する可能性）
- 深刻度: 中（アプローチ A の実装方針次第で高に変わりうる。アプローチ B は低）

### 人間承認ルートの条件

アプローチ A を将来実装する場合、以下を満たせば CEO 明示承認のもとで実装可能:

1. 修正範囲が UI 伝達層（Claude Code クライアント側）のみであり `protect-files.sh` 等のフックロジックに一切触れない
2. 修正前後で `protect-files.sh` の承認フロー（誰がいつ承認するか・バイパスできないか）が変わらないことを CISO が確認

### 過去判断との一貫性

- 2026-04-20 の Issue #361（3者承認ゲート導入）と直接関連。承認フロー変更は同判断の前提を変える可能性があるため、アプローチ A の除外が適切。
- protect-files.sh は「削除不可最低ライン」（2026-04-18 プリセット別安全性評価）と整合。


## 2026-04-25 — 不可領域チェック: --plugin-dir 自動付与シェル関数（~/.zshrc 追加）

### 判定

OK — 5分類いずれにも該当しない。通過。

### 対象 Issue

- タイトル: "vibecorp 本体で claude 起動時に --plugin-dir が自動付与されるようになった"
- 変更概要: CWD に `.claude-plugin/plugin.json` があれば `claude --plugin-dir .` に変換するシェル関数を `~/.zshrc` に追加する（vibecorp 本体開発者環境限定、導入先プロジェクトには影響なし）

### 不可領域チェック（5分類）

| 分類 | 判定 | 根拠 |
|------|------|------|
| 1. 認証 | 非該当 | hooks/*auth*、hooks/*permission*、settings.json の permissions、gh auth、ANTHROPIC_API_KEY のいずれにも触れない |
| 2. 暗号 | 非該当 | encrypt/decrypt/secret/credential/token を扱うコードへの変更なし |
| 3. 課金構造 | 非該当 | `claude -p`（headless LLM 呼び出し）ではなくインタラクティブ TUI 起動の引数補完。コスト上限パラメータ変更なし |
| 4. ガードレール | 非該当 | protect-files.sh、diagnose-guard.sh、forbidden_targets、diagnose-active スタンプ制御に触れない |
| 5. MVV | 非該当 | MVV.md の変更なし |

### 課金構造の詳細評価

不可領域の課金構造定義: `claude -p`, `npx`, `bunx` で LLM を呼ぶ箇所。本 Issue の変更は `claude --plugin-dir .`（TUI インタラクティブ起動）であり、`-p` フラグを使う自律的 LLM 呼び出しではない。自律ループや max_issues_per_day 等のコスト上限にも触れない。非該当と判定。

### 攻撃チェーン分析

- `~/.zshrc` へのシェル関数追加はユーザーのシェル環境変更。`--plugin-dir .` は CWD の `.claude-plugin/plugin.json` を読み込むため、悪意あるディレクトリで claude を起動すると意図しないプラグインが読まれる可能性はあるが、これはプラグイン機構自体の問題であり本変更固有のリスクではない
- 導入先プロジェクトへの影響なし（vibecorp 本体開発者の `~/.zshrc` のみ）
- 深刻度: 低（新規攻撃面は限定的）

### SECURITY.md 整合

問題なし。sandbox-exec レイヤ（SECURITY.md 主要記述）との関係も影響なし。

### 過去判断との一貫性

一貫。`claude -p` 呼び出しに関する過去判断（#361、プリセット別安全性評価等）はいずれも headless/自律起動に関するものであり、本 Issue とは別カテゴリ。矛盾なし。

## 2026-04-25 — 不可領域チェック: --plugin-dir 自動付与 scripts/dev.sh ラッパー方式

### 判定

OK — 5分類いずれにも該当しない。通過。

### 対象 Issue

- タイトル: "✨ feat: vibecorp 本体の開発時に --plugin-dir を手入力しなくてよくなった"
- 変更概要: vibecorp 本体リポジトリで `claude` 起動時の `--plugin-dir .` 手入力を解消するため、`scripts/dev.sh` にラッパーを用意する。`~/.zshrc` 等のユーザー設定には依存しない（`.claude/rules/public-ready.md` 準拠）

### 不可領域チェック（5分類）

| 分類 | 判定 | 根拠 |
|------|------|------|
| 1. 認証 | 非該当 | hooks/*auth*、hooks/*permission*、settings.json の permissions、gh auth、ANTHROPIC_API_KEY のいずれにも触れない |
| 2. 暗号 | 非該当 | encrypt/decrypt/secret/credential/token を扱うコードへの変更なし |
| 3. 課金構造 | 非該当 | `claude --plugin-dir .`（TUI インタラクティブ起動の引数補完）であり `claude -p`（headless LLM 呼び出し）ではない。コスト上限パラメータ変更なし |
| 4. ガードレール | 非該当 | protect-files.sh、diagnose-guard.sh、forbidden_targets、diagnose-active スタンプ制御に触れない |
| 5. MVV | 非該当 | MVV.md の変更なし |

### 前回エントリ（2026-04-25 ~/.zshrc 追加アプローチ）との差分

同日付の既存エントリは `~/.zshrc` へのシェル関数追加アプローチを対象としていた。今回は `scripts/dev.sh` にラッパーを閉じる方式であり、ユーザーグローバル設定への書き込みを行わない。影響範囲がより小さく、`.claude/rules/public-ready.md`「ローカル環境・ユーザー設定への依存禁止」にも適合している。

### 攻撃チェーン分析

- `scripts/dev.sh` 実行者が悪意あるカレントディレクトリで起動した場合、意図しないプラグインがロードされうるが、これはプラグイン機構の既知の挙動であり本変更固有のリスクではない
- 導入先プロジェクトへの影響なし（vibecorp 本体開発者限定）
- 深刻度: 低

### 過去判断との一貫性

一貫。前回の `~/.zshrc` アプローチ（OK）より保守的な設計であり矛盾なし。

## 2026-04-29 — Issue #442 knowledge 構造統一（monitoring ログ四半期集約 + protect-knowledge-direct-writes.sh パターン更新）

### 判定

承認: セキュリティリスクなし

### 対象

- `templates/claude/hooks/protect-knowledge-direct-writes.sh` — deny パターン `audit-*.md` → `*/audit-log/*.md` へ変更
- `skills/audit-security/SKILL.md`, `skills/audit-cost/SKILL.md` — 書込先を `audit-log/YYYY-QN.md` 四半期集約に変更
- `templates/claude/agents/security-analyst.md` — 記録フローを `BUFFER_DIR` 経由に明文化
- `skills/cycle-metrics/SKILL.md` — 出力先を `~/.cache/vibecorp/state/<repo-id>/cycle-metrics/` に変更
- 各種テスト・whitelist 追従

### 合議状況

security-analyst ×3 合議なし（差分が refactor 中心のため直接判定）

### メタレビュー判定

**protect-knowledge-direct-writes.sh の fail-secure 維持**

変更前: `accounting/audit-*.md` と `security/audit-*.md` の 2 ロール限定パターン
変更後: `*/audit-log/*.md` のワイルドカードパターン

評価:
- deny 対象範囲が拡大（2 ロール → 全ロール統一）。legal 等の将来ロール追加時にもコード変更不要
- `*/audit-log/*.md` は `audit-log/` サブディレクトリ直下のファイルのみにマッチし、上位ディレクトリへの誤 deny は発生しない
- realpath 正規化不能時の fail-closed ブランチにも同一パターンが適用済み（コード L50-53 で確認）
- buffer 経由許可ロジックは変更なし。fail-secure の設計が維持されている
- スタンプによる救済が `audit-log/` パターンで明示的に除外されている（コード L97-99）
- テスト（test_protect_knowledge_direct_writes.sh）にテスト8c が追加され、`audit-log/` に対するスタンプ fail-secure も検証済み

**audit-log-index.md の情報漏洩リスク**

index に格納されるのは 1 行サマリ（日付・Issue 番号・Critical/Major 件数のみ）。個別脆弱性の詳細は YYYY-QN.md に収容。既存の `audit-log.md` と同等以上の保護（buffer 経由フロー、protect フックによる直書き deny）が適用される。悪化なし。

**buffer worktree 経由フローのセキュリティ境界**

フロー変更なし。書込先パスが `accounting/audit-YYYY-MM-DD.md` から `accounting/audit-log/YYYY-QN.md` に変わったのみ。buffer 境界チェック（`$expected_prefix` による prefix 比較）は変更なし。

**新規 audit-log/ ディレクトリのアクセス制御**

`.claude/knowledge/*/audit-log/*.md` は protect フックの deny 対象に含まれる。直書き経路が封鎖されており、既存の decisions/ と同等の保護レベル。

**cycle-metrics の出力先変更**

`.claude/knowledge/` 外（`~/.cache/vibecorp/`）への移動は、git 履歴への揮発データ混入を防ぐ設計改善。protect フックの deny 対象パスから外れるが、意図的かつ適切（揮発データなので直接保存で問題ない）。

**認証・暗号・credential 変更**

変更なし。

### 脅威評価

- 深刻度: 低
- 攻撃チェーン: リスクなし。deny パターン範囲が拡大方向であり、攻撃面の縮小効果がある
- 影響範囲: 監査ログの保存経路のみ。認証・認可・暗号に影響なし

### 過去判断との一貫性

一貫。deny 対象の拡張は 2026-04-18 のプリセット別安全性評価（buffer 経由フロー前提の protect 設計）と整合する。


## 2026-04-29 — Issue #448 protect-knowledge-bash-writes.sh 実装計画メタレビュー

### 判定

差し戻し（Major 3 点を修正後に再提出）

### 対象

- 計画ファイル: `~/.cache/vibecorp/plans/vibecorp-54064690/dev_448_subagent_hook_enforcement.md`
- 多層防御（Edit/Write hook + 新規 Bash hook）でガードレールを強化する設計

### 合議状況

計画段階のため security-analyst 合議未実施。CISO 単独メタレビュー。

### 検出された脆弱性

#### M-001（Major）: 環境変数プレフィックス除去の正規表現が単一変数のみ対応

計画の正規化処理 `sed 's/^[A-Za-z_][A-Za-z0-9_]*=[^ ]* //'` は先頭 1 変数しか除去しない。`FOO=1 BAR=2 cat >> .claude/knowledge/...` のように複数の環境変数プレフィックスがある場合、`BAR=2` が残り deny パターンにマッチしなくなる。`env`/`command` ラッパー除去も未実装（`.claude/rules/shell.md` 違反）。

#### M-002（Major）: `bash -c` 経由バイパスがテスト未網羅

`bash -c "cat >> .claude/knowledge/..."` 形式は計画のテスト項目に含まれていない。agent が Bash tool に Edit/Write の両手段を持つ状況で LLM が bash -c 経由の redirect を選択した場合、hook をバイパスできる。また `&&`/`;` 複合コマンド内の後続 redirect もシーケンシャルな正規表現では検出不可。

#### M-003（Major）: buffer worktree prefix チェックが false allow 経路を持つ

計画の buffer 許可ロジックは `expected_prefix` への前方一致のみ。buffer worktree が未初期化の場合でも prefix 配下へのパスが許可される（fail-secure 原則違反）。既存の `protect-knowledge-direct-writes.sh` は `knowledge_buffer_worktree_dir` 呼び出し → abs 化 → 実在・サブパス確認の 3 段構成で正しく処理しており、新 hook も同パターンを踏襲すべき。

### Minor 指摘

- T-001: BSD `sed -i ''` 形式がテスト項目に不在
- T-002: deny メッセージ内の `${BUFFER_DIR}` が未設定時に broken になる可能性。デフォルト値指定が必要

### 攻撃チェーン分析

最大リスク経路（M-002）:
エージェントに Edit/Write を追加（Phase 3）した後でも Bash tool が利用可能 → LLM が `bash -c "cat >> .claude/knowledge/..."` を選択 → Bash hook の検出ロジックが `bash -c` 内を展開せず通過 → knowledge 直書きが発生

M-003 の false allow 経路:
buffer worktree 未初期化状態でキャッシュパスを推測した redirect → prefix チェックを通過 → deny されずに直書き

### SECURITY.md 整合

問題なし。多層防御・fail-secure 方針と設計方向は整合している。

### 過去判断との一貫性

一貫。2026-04-29 Issue #442 の「deny パターン拡大方向・buffer フロー境界維持」と整合。

### 修正後の承認条件

以下 3 点を満たした実装であれば承認:
1. M-001: 複数環境変数プレフィックスおよび `env`/`command` ラッパーの除去
2. M-002: `bash -c`/`sh -c` 内コマンド展開検査、または既知ギャップをテストで明示
3. M-003: 既存 protect-knowledge-direct-writes.sh と同等の 3 段 prefix チェック


## 2026-04-29 — Issue #448 修正コミット 41e0060 メタレビュー（実装後承認）

### 判定

承認: セキュリティリスクなし

### 対象

- コミット 41e0060（protect-knowledge-bash-writes.sh 実装 + C*O ロール Edit/Write/MultiEdit 追加）
- 差し戻し時（計画段階）の Major 3 点（M-001/M-002/M-003）の修正確認

### 合議状況

差し戻し後の修正実装レビュー。security-analyst 合議なし。CISO 単独メタレビュー（差分がガードレール強化中心・hook 新設・agent 定義変更のみ）。

### M-001/M-002/M-003 修正確認

#### M-001（複数環境変数プレフィックスおよび env/command ラッパー除去）

while ループで複数の環境変数プレフィックスを繰り返し除去する実装。実動作検証: `FOO=1 BAR=2 cat >> /tmp/test.md` → `cat >> /tmp/test.md` に正規化。env ラッパーは case 文で `env `/`command ` プレフィックスを while ループで除去し、env のフラグ（`-i`, `--ignore-environment` 等）も内側 while で剥がす。実動作検証: `env -i FOO=1 cat >> /tmp/test.md` → `FOO=1 cat >> /tmp/test.md` → `cat >> /tmp/test.md`（2 段処理）。**修正確認: OK**

#### M-002（bash -c 内コマンド展開、または既知ギャップのテスト明示）

case 文で `bash -c "..."` / `bash -c '...'` / `sh -c "..."` / `sh -c '...'` の 4 形式を展開。実動作検証: `bash -c "cat >> /tmp/knowledge/decisions/2026-Q2.md"` → inner `cat >> /tmp/knowledge/decisions/2026-Q2.md` に展開され、リダイレクト検出で捕捉。複合コマンド `cmd1 && echo foo >> ...` も展開後に検出可能。

既知ギャップ（`bash -c $'...'`、shell function 経由、eval 経由）はコメントで明示され、テスト 11 で「通過（既知ギャップ）または deny（安全側）どちらも pass 扱い」として明文化。Edit/Write 層（protect-knowledge-direct-writes.sh）+ agent 定義の Edit/Write 強制でカバーされる設計。**修正確認: OK**

#### M-003（3 段 prefix チェック）

実装: `[ -n "$buffer_dir" ] && [ -n "$abs_detected_path" ]` で外側 if → abs_buffer_dir 正規化 → `expected_prefix` 前方一致（vibecorp_cache_root 経由で XDG 尊重）→ detected_path が buffer サブパスか確認。buffer_dir 空（worktree 未初期化）の場合は外側 if が false → deny へ落ちる（fail-secure）。abs_detected_path 空（realpath/python3 両方不在）の場合も deny。既存 protect-knowledge-direct-writes.sh と同等の 3 段ガードを踏襲。abs_detected_path 非空チェックを追加しており既存 hook より厳密。**修正確認: OK**

### 多層防御の設計強度評価

| 層 | 実装 | 強度 |
|----|------|------|
| Edit/Write/MultiEdit 層 | protect-knowledge-direct-writes.sh（file_path マッチ） | 高（ツール引数を直接検査） |
| Bash 層 | protect-knowledge-bash-writes.sh（コマンド文字列検査） | 中（コマンド正規化の限界あり） |
| エージェント定義 | 6 ロールに Edit/Write/MultiEdit を tools 宣言 + Bash redirect 禁止明文化 | 中（LLM の選択をツール制約で誘導） |

3 層の組み合わせで、単一層のバイパスが知識領域への直書きに直結しない設計。既知ギャップ（`bash -c $'...'` / shell function / eval）は Edit/Write 層が拾う設計として文書化済み。

### 認証・暗号・credential 変更

なし。変更ファイルは hook スクリプト、lib ヘルパー、agent 定義、settings.json（hook 登録）、テスト、docs に限定。

### SECURITY.md 整合

問題なし。fail-secure・多層防御の方針（docs/design-philosophy.md に明文化）と整合。

### 過去判断との一貫性

差し戻し時（計画段階）の条件 3 点を全て充足。2026-04-29 Issue #442 承認（deny パターン拡大方向・buffer フロー境界維持）と矛盾なし。

### 攻撃チェーン評価

差し戻し時の最大リスク経路（M-002）はテスト 8「bash -c "..." 展開」で検出されることを実動作確認済み。M-003 の false allow 経路は buffer_dir 空時に deny へ落ちることを確認済み。新たな攻撃経路の導入なし。

深刻度: 低（防御追加のみ、攻撃面拡大なし）
