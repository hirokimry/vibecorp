# CPO 判断記録 2026-Q2

## 2026-04-02 — protect-branch.sh を全プリセット（minimal 以上）に追加

- **判断**: `protect-branch.sh` をファイル保護型フックとして全プリセットに配置し、README.md のフック一覧・プリセットテーブル・settings.json サンプルの3箇所に反映した
- **根拠**: メインブランチへの直接 Edit/Write/git commit は「規律の自動化」バリューに反する。`protect-files.sh` と同カテゴリ（ファイル保護型）として minimal 以上で有効化するのが一貫性のある判断。プリセットテーブルでは `protect-files` の直後に並べ、settings.json サンプルでも同マッチャー内の隣接エントリに配置した
- **代替案**: standard 以上での提供も検討したが、ブランチ保護はチーム規模に依らず基本的な安全網であり、個人開発でも有効なため minimal 以上が適切


## 2026-04-02 — review-gate.sh の README 未反映を要更新と判定

- **判断**: Issue #209 で追加された `review-gate.sh` は README のフック一覧・スタンプ対応表・settings.json 掲載内容の3箇所に反映が必要と判定した
- **根拠**: README は「インストールされるもの」の Source of Truth として機能しており、フック一覧に掲載されていないフックはユーザーが存在を認識できない。透明性のバリュー（「全ての判断根拠をルール・設定ファイルとして可視化する。ブラックボックスを作らない」）に反する状態である
- **代替案**: 新フック追加時に README 更新を省略する方式も考えられるが、README がフックの公式一覧として機能している以上、省略は情報の欠落に当たる


## 2026-04-02 — command-log.sh をゲートフックではなくログ専用フックとして実装

- **判断**: `command-log.sh` は実行の可否を判定するゲートフック（ブロック型）ではなく、全コマンドをログに記録するだけの観察専用 PreToolUse フックとして実装した。ブロック判定は行わず、終了コード 0 で常に通過させる
- **根拠**: ゲートフックとして実装した場合、ユーザーが想定外のコマンドを実行できなくなる。これは「導入の手軽さ」バリュー（「設定の迷いをなくす」）に反し、初期導入の摩擦を高める。まず観察フェーズとしてログを蓄積し、`/approve-audit` スキルで棚卸しした上で allow リストに追加するという「観察→承認」の二段階フローが、「導入の手軽さ」と「規律の自動化」の両立を実現する
- **代替案**: 初回から allow リスト外コマンドをブロックする方式も検討したが、チームによって許可コマンドのセットが大きく異なるため、一律にブロックすると導入直後に大量の false positive が発生し UX が損なわれる。段階的に規律を育てる設計のほうが「段階的成長」バリューにも合致する


## 2026-04-11 — /spike-loop を full プリセット専用スキルとして承認

- **判断**: `templates/claude/skills/spike-loop/SKILL.md` で追加された `/spike-loop` スキルを full プリセット専用スキルとして承認し、README.md のスキル一覧テーブルと「主要スキルの使い方」セクションに反映した
- **根拠**: `/spike-loop` は `/ship-parallel` の E2E 検証に SM エージェント（TeamCreate での並列実行）を必要とするため、full プリセット専用の位置付けは妥当。修正の自動適用を行わず分析レポートのみ出力する Phase 1 設計は、「暴走を防止する」という既存の `/diagnose` と同じ原則（起票と実装の分離）に沿っており、MVV の「規律の自動化」バリューと整合する。README への追記は「透明性」バリュー（「全ての判断根拠をルール・設定ファイルとして可視化する」）の観点から必須
- **代替案**: Phase 2（修正の自動適用）まで README 掲載を保留する案も考えられるが、スキル自体は既に追加済みであり、ユーザーが存在を認識できない状態は情報の欠落に当たるため、Phase 1 の段階で反映することが適切


## 2026-04-16 — CEO/主Claude役割定義をvibecorp本体に書くことを却下

- **判断**: 「ユーザー（hirokimry）= CEO兼ファウンダー、主Claude = 秘書/参謀/執行役」を `.claude/CLAUDE.md` または `.claude/rules/roles.md` に明記する提案を却下した
- **根拠**: vibecorp は「あらゆるリポジトリに導入されるプラグイン」であり、特定個人の識別情報（メールアドレス等）をプロダクト本体の設定ファイルに書くことは Mission「あらゆるリポジトリで再現性のある開発を実現する」と矛盾する。また `public-ready.md` ルール（個人情報の混入禁止）への明示的な違反でもある。主Claudeと対話ユーザーの役割関係の定義は、vibecorp が提供するプラグインのスコープ外であり、導入先プロジェクトのオーナーが各自のCLAUDE.mdで設定すべき事項である
- **代替案**: templates/ 配下に roles.md のサンプルテンプレートを配置し、導入後に自身のCLAUDE.mdへ記述することを推奨するガイダンスをREADMEに追記する形であれば許容可能。ただし個人情報は含めない


## 2026-04-16 — ship-parallel / autopilot を full プリセット専用に格下げ承認（Issue #308）

- **判断**: `/ship-parallel` と `/autopilot` を minimal/standard プリセットから物理削除し、full プリセット専用に格下げする。`/spike-loop` は既に full 専用扱いだが install.sh で物理削除されていなかったため同時に整理する
- **根拠**: #293 隔離レイヤ計画で「隔離は full プリセットのみ」が確定した。隔離が効かない minimal/standard でヘッドレス並列スキルを開放するのは、MVV の「規律の自動化 — レビュー・保護・ルールをフックとスキルで強制する。人の意志力に頼らない」バリューに矛盾する。リスクある操作は対応する保護機構（隔離レイヤ）が整った環境に限定すべき
- **代替案**: vibecorp.yml のオプトインフラグで個別有効化を許容する案も考えられるが、デフォルトで安全な構成を強制する設計方針を優先した。SKILL.md 内のソフトガードのみで制御する案は install.sh で配置してしまう以上バイパスリスクが残るため、物理削除によるハード制限を採用した


## 2026-04-16 — specification.md の隔離レイヤ記述を Phase 1 PoC の opt-in 設計に整合させる

- **判断**: `docs/specification.md` L27 の「隔離レイヤ（#293）が full でしか有効化されないため」という記述を削除・修正し、Phase 1 PoC として `VIBECORP_ISOLATION=1` 環境変数による opt-in（プリセット非依存）であることを明記した。full 専用スキルの理由説明は「課金リスクを伴う大規模並列実行のため」という実態に即した表現に変更した
- **根拠**: PR #317 / Issue #309 で追加された sandbox-exec 隔離レイヤは `VIBECORP_ISOLATION=1` による opt-in 設計であり、full プリセット限定ではない。仕様書の記述が実装と乖離していることは MVV「透明性」バリュー（「全ての判断根拠をルール・設定ファイルとして可視化する。ブラックボックスを作らない」）に反する。また decisions.md の 2026-04-16 判断（#308 対応）では「隔離は full プリセットのみ」という方針だったが、Phase 1 PoC は install.sh 未連携で templates/ に配置されるのみのため、プリセット制約とは切り離して記述するのが正確
- **代替案**: Phase 3 の install.sh 連携時まで記述変更を保留する案も考えられるが、すでに実装が存在する以上、現状を正確に文書化するほうが透明性の観点から適切


## 2026-04-16 — Phase 3a Issue（install.sh macOS 統合）をプロダクト方針に合致と判定

- **判断**: Issue「Phase 3a - install.sh macOS 統合（隔離レイヤ #293 先行）」を OK と判定した。full プリセット限定での配置、minimal/standard では既知ファイルのみ `rm -f` で除外しディレクトリは `rmdir` で空のときだけ削除（ユーザー追加ファイル保護）という設計、Linux 分割は #310 完了後という段階的リリース戦略のいずれも方針に整合する
- **根拠**: 「導入の手軽さ」バリュー（install.sh だけで隔離が動く状態の実現）および「段階的成長」バリュー（Linux 依存の完了を待たず macOS 分を先行）に合致。full 専用のハード制限は decisions.md 2026-04-16 (#308) の判断と一貫している。specification.md の「Phase 1 では install.sh 未連携」という記述も本 Issue 完了で更新される予定であり、透明性バリューの観点からも問題なし
- **代替案**: Phase 3 本体 (#311) として一括実装する案も考えられるが、Linux bwrap (#310) への依存でブロックされ続けるため、macOS 先行分割が適切


## 2026-04-16 — Issue #320 TUI ハング修正（claude.sb 境界拡張 + claude-real symlink）を OK と判定

- **判断**: `dev/320_isolation_tui_fix` の変更（claude.sb への 4 境界追加、install.sh への `setup_claude_real_symlink()` 追加、SECURITY.md の既知制約更新）をプロダクト方針に合致と判定した
- **根拠**: full + macOS のみで動作するハード制限が実装されており、decisions.md 2026-04-16 (#308) の「物理削除によるハード制限」方針と一貫。cleanup 処理への `claude-real` 除去追加でダウングレード残滓も防止。SECURITY.md に境界追加の根拠・既知制約・再検証推奨を明記しており「透明性」バリュー適合。specification.md L27 との整合も確認済み。Public Ready ガードレール違反なし
- **代替案**: なし（バグ修正かつスコープ限定変更のため）


## 2026-04-17 — README.md ゲートスタンプのパスを XDG キャッシュ配下に更新（Issue #326）

- **判断**: README.md L220 の説明文と L224〜227 のテーブル列を、旧パス `$CLAUDE_PROJECT_DIR/.claude/state/*` から新パス `~/.cache/vibecorp/state/<repo-id>/<stamp-name>-ok` に更新した
- **根拠**: PR #327 / Issue #326 でゲートスタンプの保存先が XDG Base Directory 仕様準拠の `${XDG_CACHE_HOME:-$HOME/.cache}/vibecorp/state/<repo-id>/` に移行された。README が旧パスを記載したままでは「透明性」バリュー（「全ての判断根拠をルール・設定ファイルとして可視化する。ブラックボックスを作らない」）に反する。また分離単位の説明も「CLAUDE_PROJECT_DIR が worktree ごとに異なる」から「repo-id = sanitized basename + sha256 先頭8桁によるリポジトリ単位分離」に実態が変わったため、同時に更新した
- **代替案**: なし（仕様変更に追随するドキュメント修正のため）


## 2026-04-17 — /login がサンドボックス下で無効になるバグ修正を OK と判定（Issue #329）

- **判断**: `VIBECORP_ISOLATION=1` 環境下で `/login` が失敗するバグ（claude.sb が `.claude.json.lock` / `.claude.json.tmp.*` サイドカーを deny）の修正 Issue をプロダクト方針に合致と判定した
- **根拠**: claude.sb のサイドカーパスカバー漏れという実装不備の修正であり、#325 回帰の修正。SECURITY.md の書込境界表更新・テスト追加が受入基準に含まれており「透明性」「規律の自動化」バリューと整合する。修正手法は #320（TUI ハング修正）と同一パターン（claude.sb への境界追加）であり一貫性がある
- **代替案**: なし（バグ修正かつスコープ限定変更のため）


## 2026-04-18 — decisions.md のインデックス+アーカイブ2段構成への分割を OK と判定

- **判断**: `.claude/knowledge/{role}/decisions.md` を `decisions-index.md`（毎回読む目次）と `decisions/2026-Q*.md`（関連時のみ読むアーカイブ）の2段構成に分割する Issue をプロダクト方針に合致と判定した
- **根拠**: サブエージェントは毎回まっさらな別コンテキストで起動されるため、全量ロード（CISO の場合 ~29KB）はコンテキストコスト・レスポンス速度の両面でユーザーに悪影響を与える。インデックス参照 → 必要時のみアーカイブ読込という設計は「導入の手軽さ」バリュー（設定の迷いをなくす/スムーズな動作）および「規律の自動化」バリュー（仕組みで効率を担保）と整合する。インデックスにより判断サマリーが一覧できることは「透明性」バリューの向上にも寄与する
- **代替案**: 一定行数を超えたら手動アーカイブをユーザーに案内する方式も考えられるが、エージェント定義変更で全起動時に自動適用される構造改善のほうが「規律の自動化」に合致する


## 2026-04-18 — プランのUX軸再定義（minimal/standard/fullの価値提案を明確化）

- **判断**: minimal/standard/full の価値提案を「機能量・組織規模」軸から「ユーザーの承認体験・自律度」軸に再定義することを適切と判断した。保護系フックはどのプランでもSKIP不可。ゲート系フックはstandardで必須、fullはsandbox有効時に任意。ヘッドレス並列はfull専用を維持。`docs/specification.md` のプリセット表に「ユーザーの役割」列追加を推奨。
- **根拠**: 現行の specification.md プリセット表は「追加されるもの（機能一覧）」のみで、ユーザーが何を体験するかを記述していない。これは MVV「導入の手軽さ — 設定の迷いをなくす」バリューに反する（ユーザーが自分に合ったプランを選べない）。「段階的成長」バリューも「機能を足す」ではなく「承認を減らして自律度を上げる」体験として再解釈することで、プランの移行動機が明確になる。
- **代替案**: プランを2段階（minimal/full）に統合する案も考えられるが、standardの「ゲート型チーム承認体験」はミドルウェア的に必要であり、既存ユーザーへの互換性破壊を避けるため3段階を維持する。


## 2026-04-18 — team-auto-approve.sh 完全削除と承認フロー非介入思想の明文化を OK と判定

- **判断**: `team-auto-approve.sh` をリポジトリから完全削除し、vibecorp が「承認フローに介入しない」設計思想を `docs/design-philosophy.md` と README.md に明文化する Issue をプロダクト方針に合致と判定した
- **根拠**: team-auto-approve.sh は Claude Code の承認ダイアログをバイパスする hook であり、「ブラックボックスを作らない」という透明性バリューと本質的に相反する。full + sandbox 有効なら承認ダイアログ自体が発生しないため、このフックが解決していた問題はプリセット設計で対処できる。また「規律の自動化」バリューは規律の強制を意図しており、規律の迂回自動化は趣旨外。削除により振る舞いが Claude Code の素の挙動と一致し、「Source of Truth は Claude Code の素の挙動」という CEOとの合議方針と整合する。2026-04-18 のプランUX軸再定義（承認体験・自律度軸）の実装レベルでの具体化でもある
- **代替案**: full + sandbox 無効時のみ team-auto-approve を残す案も考えられるが、「何が自動承認されているか分からない」不透明性は残り続けるため採用しない。full + sandbox 無効で並列実行したユーザーは承認手間を自己責任として受け入れるか settings.local.json で自ら調整する前提とした


## 2026-04-18 — auto 体験射程と SKIP 性マトリクスの specification.md 追加を OK と判定

- **判断**: `docs/specification.md` に「プランごとの auto 体験射程（公式サポート・ユーザー裁量）」列と「フック SKIP 性マトリクス」新規セクションを追加する Issue をプロダクト方針に合致と判定した。
- **根拠**: 現行の specification.md プリセット表は機能一覧のみで、「ユーザーが何を体験するか」が未定義だった。これは「導入の手軽さ — 設定の迷いをなくす」バリューに反しており、decisions.md 2026-04-18「プランUX軸再定義」で既に方針承認済みの文書化作業にあたる。SKIP 性マトリクスによる保護・ゲート系フックの明文化は「規律の自動化」「透明性」バリューの両立。全体として矛盾なし。
- **代替案**: 検討なし（既存承認方針の文書化であるため）。
- **実装時の注意**: (1) design-philosophy.md 側のプリセット表との記述矛盾が生じないよう、実装担当は両ファイルを同時確認すること。(2) SKIP 性マトリクスには `block-api-bypass` と `command-log` を「必須」として明記すること（Issue 本文の表に掲載漏れがある）。


## 2026-04-18 — max_issues_per_run 等の上限ガードレール値を cost-analysis.md に明記する Issue を OK と判定

- **判断**: `docs/cost-analysis.md` に「自律実行の上限ガードレール」セクションを追加し、`max_issues_per_run` / `max_issues_per_day` / `max_files_per_issue` のデフォルト値・根拠・超過時挙動を明記する Issue をプロダクト方針に合致と判定した。
- **根拠**: 上限値が vibecorp.yml のコメントにのみ存在し cost-analysis.md に記載されていない状態は、「全ての判断根拠をルール・設定ファイルとして可視化する。ブラックボックスを作らない」という透明性バリューに反する。autonomous-restrictions.md は `docs/cost-analysis.md` を「課金構造の変更は自律実行禁止」対象として保護しているが、その保護先が空欄では意味をなさない。本 Issue はガードレールが守る具体値を文書化する作業であり、ガードレール設計の完結にあたる。
- **代替案**: vibecorp.yml に値を書くだけで十分という案も考えられるが、cost-analysis.md が specification.md から明示的に誘導されている公式ドキュメントである以上、そちらへの記載が必要。
- **実装時の注意**: (1) vibecorp.yml の値を正として cost-analysis.md に転記すること（先行して異なる値を書かない）。(2) autonomous-restrictions.md への変更は相互リンク追加（1行程度）に留め、ルール本文の実質的な改変はしないこと。


## 2026-04-18 — install.sh への full プリセット用課金警告・sandbox 推奨警告実装を OK と判定

- **判断**: install.sh に「ANTHROPIC_API_KEY 検出時の従量課金警告（警告A）」と「macOS + sandbox 未有効時の推奨警告（警告B）」を full プリセット専用で実装する Issue をプロダクト方針に合致と判定した。
- **根拠**: `docs/specification.md` L24 には「ANTHROPIC_API_KEY 従量課金に到達しうる」、L27 には「install.sh が課金警告を表示する」と既に記載されているが install.sh に実装がない状態は仕様と実装の乖離であり、「透明性」バリュー（ブラックボックスを作らない）に反する。また decisions.md 2026-04-18「team-auto-approve.sh 削除」で「full + sandbox 無効での並列実行はユーザーの自己責任」と明記したが、その前提は install.sh 時点での告知によって初めて成立する。告知なしに自己責任を問うのは「導入の手軽さ — 設定の迷いをなくす」バリューに反する。
- **代替案**: README のみへの記載で代替する案も考えられるが、「規律の自動化 — 人の意志力に頼らない」バリューに基づき、ユーザーが能動的に読まなくても警告が届く install.sh への実装が適切。
- **実装時の注意**: (1) `docs/cost-analysis.md` に警告条件を記述する場合、同ファイルは autonomous-restrictions.md が自律実行禁止対象に指定しているため、変更時は人間の承認を経ること。(2) 非対話環境では `[ -t 0 ]` または `$CI` で tty 判定を行い、確認プロンプトは出さず警告テキスト（stderr）のみ出力して実行継続すること。この動作を tests/test_install.sh で明示的に検証するケースとして追加すること。


## 2026-04-18 — test_install.sh シャード分割・CI matrix 並列化を OK と判定

- **判断**: `tests/test_install.sh` を論理単位（args/preset/lock/update）の 3〜4 ファイルに分割し、CI workflow matrix を `{os, shard}` 2次元に拡張して並列実行する Issue をプロダクト方針に合致と判定した。
- **根拠**: CI 時間の 7分→2-3分 短縮は開発者の PR フィードバックループ改善であり、「導入の手軽さ」バリュー（設定の迷いをなくす/スムーズな開発体験）に直結する。テスト分割により CI 失敗時の原因特定が容易になることは「透明性」バリュー（ブラックボックスを作らない）の向上にも寄与する。プリセットスコープ・機能仕様・autonomous-restrictions.md の禁止領域（認証・暗号・課金・ガードレール・MVV）のいずれにも影響しない純粋なインフラ改善。
- **代替案**: 検討なし（スコープが明確で MVV 整合性に問題なし）。


## 2026-04-18 — CEO への報告は「動作で語る」規約新設（communication.md）を OK と判定

- **判断**: `docs/ai-organization.md` への基本思想追記、`.claude/rules/roles.md` への CEO セクション性質追記、`.claude/rules/communication.md` 新設、スキル SKILL.md 4 件への参照追加、個人メモリ整理をプロダクト方針に合致と判定した
- **根拠**: エージェントが関数名・diff を羅列する報告スタイルは CEO が変更内容を読み取れない不透明な状態を作っており、「透明性」バリュー（ブラックボックスを作らない）に反する。動作主語への統一をルールファイル（永続）として強制することは「規律の自動化 — 人の意志力に頼らない」の実践。個人メモリ（揮発性）からプロジェクトルール（永続性）への昇格はまさにこのバリューの体現。ai-organization.md の基本思想「ユーザーは CEO として意思決定に集中する」の具体化でもある。2026-04-16 却下判断（CEO/主Claude役割定義）とは異なり、特定個人の識別情報を含まないため public-ready.md 抵触なし
- **代替案**: 個人メモリのみに残す案も考えられるが、エージェントが毎回まっさらなコンテキストで起動される特性上、メモリは揮発的であり rules/ への昇格が確実


## 2026-04-18 — /spike-loop 廃止と承認フロー非介入思想の適用を OK と判定

- **判断**: `/spike-loop` スキルの完全廃止、Issue #263（Phase 2 自動修正）の won't fix 化、`docs/design-philosophy.md` への「検証スキルへの思想適用」追記をプロダクト方針に合致と判定した
- **根拠**: spike-loop Phase 2 設計（permission hook の allow リスト自動拡張）は team-auto-approve.sh と同質の「承認フロー書き換え hook」であり、decisions.md 2026-04-18「team-auto-approve.sh 完全削除」で確立した「承認フロー非介入」思想と直接矛盾する。Phase 1 の存在意義も、#308（full 専用格下げ）・#336（承認フロー非介入確定）以降の運用想定ではどのモードでも spike-loop が検出したい事象が起きない、または自己責任扱いとなるため消失している。廃止はこの矛盾を解消する正しい判断。design-philosophy.md への追記により根拠が可視化され「透明性」バリューと整合する。なお 2026-04-11 の「full 専用として承認」は本判断により後続決定で更新されることになるため、歴史的記録として残しつつ廃止に至った経緯が decisions.md 上でトレースできる
- **代替案**: full + sandbox 環境のみで残す案も考えられるが、「承認フローに介入しない」思想はプリセット・sandbox 有無に関わらず設計原則として適用すべきであり、条件付き残存は思想の一貫性を損なう


## 2026-04-18 — FULLプリセット正式化・Proプラン・Issue親子関係・自動close提案のCPOレビュー

- **判断**:
  - FULLプリセット正式化: 条件付きGo（課金警告 + install.sh 整備が前提。既に Issue 承認済み）
  - Proプランの新設: No-Go。Issue親子関係・ワークツリー連携は full プリセットの機能として追加。4段目プランは「導入の手軽さ」バリューに反する
  - Issue親子関係: 条件付きGo。GitHub sub-issue APIを使い、/plan-epic スキルで親子構造を生成する体験として full 専用実装
  - 親ブランチへの子PR自動マージ: No-Go。2026-04-18確立の「承認フロー非介入」思想と矛盾。ブランチ構造提案（feature/epic-xxx からの派生）に留める
  - feature ブランチ自動close: Go。[方針転換済み → 下記「GHA テンプレート配布方式に変更」エントリ参照]
- **根拠**:
  - Proプラン不要: MVV「段階的成長」が3段階設計。4段目追加は「設定の迷いをなくす」に逆行。2026-04-18「プランUX軸 = 承認体験・自律度」再定義と整合しない
  - 自動マージ No-Go: 「承認フローに介入しない」設計原則（2026-04-18確立）と同質のリスク
  - 自動close Go: 体験として自然（後続判断により GHA テンプレート方式に変更）
- **代替案**: Proプランについて、機能課金軸（月額）での提供は考えられるが、vibecorp は OSS プラグインという性質上、プリセットによる段階提供がアーキテクチャに合っている


## 2026-04-18 — feature ブランチ自動close: GHA テンプレート配布方式に変更（/ship 追加案を撤回）

- **判断**: close 方式を「/ship スキルへの gh issue close 追加」から「GHA テンプレート配布（`templates/github/workflows/close-on-feature-merge.yml`）」に変更。本方式を OK と判定した
- **根拠**:
  - /ship 追加案は CEO 指摘で撤回（スコープ違反 + `Refs #xxx` 等の暴発リスク）
  - GHA 方式は `Closes/Fixes/Resolves` のみ厳密パースするため暴発リスクを排除（「規律の自動化」）
  - `.github/workflows/` に可視ファイルとして配置されるため動作が分かる（「透明性」）
  - LLM 非介在・コストゼロ（「導入の手軽さ」）
  - full 限定オプトイン・既存 workflows 上書きなし（decisions.md 2026-04-16 #308 と一貫）
- **前判断**: 同日「FULLプリセット正式化」エントリの自動close 行が旧方針。本エントリが後続決定として優先


## 2026-04-18 — /issue への3者フィルタ追加・/autopilot のラベル縛り撤廃を OK と判定

- **判断**: `/issue` スキルのステップ5を CPO 単体から CISO+CPO+SM の3者フィルタに拡張し、`/autopilot` の `--label "diagnose"` 縛りを撤廃して全 open Issue を対象にする Issue をプロダクト方針に合致と判定した
- **根拠**: 現行の「diagnose ラベル縛りが不可領域チェックの代替として機能している」設計は、手動起票した安全な Issue を自律ループから不当に締め出す一方、手動起票した不可領域 Issue をブロックできないという非対称な欠陥を持つ。フィルタを起票側（入口）に集約することは「レビュー・保護・ルールをフックとスキルで強制する」という規律の自動化バリューの体現。autonomous-restrictions.md の人間承認ルートも `/issue` 経由での起票に同様の保護が必要という解釈を支持する。「透明性」バリューの観点でも、判断根拠が `/issue` 実行時点に集約されることで可視化が向上する
- **実装上の注意**: `/autopilot` ステップ4「Issue が0件なら /diagnose 実行」のトリガー条件を、「diagnose ラベル付き Issue が0件」から「全 open Issue が0件」に変更することの影響を SKILL.md 改定時に明示的に記述すること。`diagnose` ラベルは起票経路識別用として残すが、ship 可否の判定には使わない旨も SKILL.md に明記すること
- **代替案**: `/autopilot` 側にも重複フィルタを残す案（起票側・ship側の両方でチェック）も考えられるが、責務の分散はロジックの重複とメンテナンスコストを生むため、入口への一本化が適切


## 2026-04-18 — 全スキル Plugin 化（#358）を条件付き GO と判定

- **判断**: Issue #358（全26スキルを `.claude-plugin/` へ移行、`/vibecorp:xxx` 名前空間導入）を条件付き GO と判定した。条件は既存3条件（design-philosophy.md 改訂・CTO 技術確認・公式 URL 明示）に加えて以下を追加: (4) 互換レイヤスタブは「移行先コマンド名が分かる形」で実装、(5) `docs/migration/` に移行ガイド作成、(6) 互換レイヤの廃止予定を明記
- **根拠**:
  - 「独自名前空間を持たない」原則の本来の意図は「Claude Code が認識しないパスを作らない」であり、`.claude-plugin/` は公式規約パスのため禁止対象に該当しない。「公式規約パスだから OK」は自己正当化でなく原則の意図に忠実な解釈
  - 全統一（ハイブリッド案の拒否）は「設定の迷いをなくす」バリューと整合。命名衝突の有無で扱いを変えるとユーザーが毎回判断を強いられる
  - 互換レイヤの廃止予定未定義は「導入の手軽さ」バリューに反する（deprecated 状態の永続化）
  - 互換スタブに移行先が明示されていないと既存ユーザーが次のアクションを取れない（透明性バリュー）
- **代替案**: ハイブリッド案（衝突スキルのみ移行）は管理コスト増・UX の一貫性喪失のため不採用。Phase 3 削除タイムライン確定まで本 Issue を保留する案も考えられるが、移行ガイドを成果物に含める条件付きで先行可能と判断


## 2026-04-18 — worktree 経由 Edit/Write を許可する protect-branch.sh の仕様明確化（Issue #296）

- **判断**: `protect-branch.sh` の Edit/Write 経路において、`tool_input.file_path` の dirname を `git -C` に渡して worktree 側のブランチを判定する修正を、「main ブランチでの直接作業禁止」仕様の正しい実装と判定した。Bash 経路（`tool_input.command` 経由）は cwd 依存のまま残し、`docs/known-limitations.md` に既知制限として明記する対処方針を承認した
- **根拠**:
  - worktree は main とは別ブランチで作業する仕組みであり、worktree 内の Edit/Write が main への直接作業とみなされていたのは誤検知。修正はフックの意図（main での直接作業を禁止する）を正確に実装するものであり、仕様の新設ではなく正しい解釈への収束
  - Bash 経路を残す判断は、shell コマンドのパース複雑性（compound command 分解・quote-aware 処理）に対する実装コストとリスクのバランスによる。`/commit` スキル経由の `cd <worktree> && git commit` 形式を使えば回避可能であり、ユーザーへの強制を最小限に抑えた適切なトレードオフ
  - `docs/known-limitations.md` への明記は「全ての判断根拠をルール・設定ファイルとして可視化する。ブラックボックスを作らない」という透明性バリューの体現。制限を隠さず可視化することで、ユーザーが回避策を取れる
- **代替案**: Bash 経路も `tool_input.command` から worktree パスを抽出して判定する方式も技術的には可能だが、quote-aware な compound command パースは実装リスクが高く、誤検知・バイパスのリスクを増やす。`/commit` スキル経由という既存の回避経路が十分に機能するため、over-engineering を避けた設計が適切
- **関連**: Issue #296, #258（teammate Bash compound command 制限）


## 2026-04-18 — 配布バグ軽量修正5件（Issue #360 抜粋）を OK と判定

- **判断**: §0-2（templates/CLAUDE.md.tpl にCOO役割追加）、§1-1（claude-real ハードコードパス除去）、§1-3（autonomous-restrictions.md パス修正）、§1-4（design-philosophy.md.tpl 追加）、§1-5（幽霊リンク削除）の5件をプロダクト方針に合致と判定した
- **根拠**:
  - §1-1 のハードコードパス（/Users/hiroki/.local/bin/claude）は `public-ready.md`（ローカル環境依存禁止）の明示的違反であり、除去は必須
  - §0-2・§1-3〜§1-5 はいずれも「透明性」バリュー（ブラックボックスを作らない）に反する配布先での不整合・リンク切れ・パス誤りの修正であり、方針と完全に整合する
  - §0 と §1-2 を別 Issue に切り出す判断はスコープの明確化として適切。親 Issue #360 をクローズしない方針も残件トラッキングとして正しい
- **代替案**: なし（既存バグの修正であり設計判断を伴わない）


## 2026-04-18 — .claude/.gitignore テンプレート配布化 + 配布系アーキテクチャ再設計 Issue を OK と判定

- **判断**: `templates/claude/.gitignore.tpl` 新設・`copy_rules` 相当フローへの切り替え、他 heredoc 生成ファイルの templates/ 移管または残す基準の明文化、tracked 化 artifact の untrack 移行ロジック追加、Source of Truth 一本化原則の `docs/design-philosophy.md` 記載をプロダクト方針に合致と判定した
- **根拠**: 現行の heredoc 方式は `templates/` に source file がなく「全ての判断根拠をルール・設定ファイルとして可視化する」透明性バリューに反する。実害（vibemux PR #25 で `/Users/hiroki/.local/bin/claude` ハードコードパスが commit 混入）が発生しており、`public-ready.md`（特定マシンパスのハードコード禁止）の構造的根本原因への対処として必要。decisions.md 2026-03-22「install.sh が .gitignore 自動生成を担う」設計方針は維持したまま、heredoc → templates/ リファクタリングで透明性を回復する。tracked 化 artifact を untrack する移行ロジックは「人の意志力に頼らない」規律の自動化バリューの実践
- **代替案**: heredoc 方式を継続し README に手順案内を記載するだけの方式も考えられるが、「指示を読まずに進める開発者が多い」という 2026-03-22 の判断根拠を踏まえ、自動化が確実



## 2026-04-20 — Issue #361 コード変更に対する README.md の要更新を判定・実施

- **判断**: Issue #361（/issue と /autopilot の責務分離 + 3者承認ゲート導入）のコード変更に対し、README.md の2箇所が旧仕様のまま残っていたため「要更新」と判定し、CPO 管轄として修正を実施した
- **修正箇所**:
  1. README.md  の説明文: 「CPO エージェントが MVV・プロダクト方針との整合をチェック」→「CISO・CPO・SM の3者が認証/暗号/課金/ガードレール/MVV の5分類で安全性と方針整合をチェック」に変更
  2. README.md  の説明文: 「全 open Issue を対象に実行（ラベルによる絞り込みなし）」の旨を追記し、diagnose ラベル縛りが撤廃されたことを明示
- **根拠**: README.md はスキルの挙動を説明する公開ドキュメントであり、「透明性」バリュー（「全ての判断根拠をルール・設定ファイルとして可視化する。ブラックボックスを作らない」）に基づき実態との乖離を解消した。specification.md には /issue・/autopilot の詳細仕様が記載されていないため、specification.md の矛盾は発生しない
- **代替案**: なし（仕様変更への追随ドキュメント修正であり設計判断を伴わない）


## 2026-04-25 — Plugin 名前空間 Phase 3: `.claude/skills/` 互換スタブ廃止を OK と判定

- **判断**: 全 26 スキルを `skills/`（Plugin ルート）に一本化し、`.claude/skills/` 互換スタブを完全廃止する Phase 3 をプロダクト方針に合致と判定した。`install.sh --update` で既存プロジェクトの旧スタブを自動クリーンアップする仕組みも OK。
- **根拠**:
  - 2026-04-18「全スキル Plugin 化（#358）条件付き GO」の条件 (6)「互換レイヤの廃止予定を明記」が Phase 2 移行ガイドで達成済み。Phase 3 はその計画された廃止の実行であり、過去判断と整合する
  - `.claude/skills/` スタブと `skills/` の二重管理状態は「設定の迷いをなくす」（導入の手軽さバリュー）に反しており、一本化による解消は正しい方向
  - `install.sh --update` による自動クリーンアップは「規律の自動化 — 人の意志力に頼らない」バリューの体現。既存ユーザーが手動でスタブを削除する手間を排除する
  - スタブ廃止後は `/vibecorp:xxx` のみが有効な呼び出し方法となり、命名の一貫性が保たれる。これは「透明性」バリュー（ブラックボックスを作らない）に寄与する
- **代替案**: 互換スタブを一定期間（例: 1四半期）残して段階廃止する案も考えられるが、スタブは転送のみで機能を持たないため残存による UX 向上がなく、二重管理のコストのみが残る。`install.sh --update` で自動移行できる以上、即時廃止が「導入の手軽さ」バリューに合致する
- **関連**: Issue #358 Phase 3, 2026-04-18「全スキル Plugin 化条件付き GO」


## 2026-04-22 — teammate 承認ダイアログのスマホ remote app 不可視問題（バグ修正 Issue）を OK と判定

- **判断**: teammate が `.claude/` 配下を編集しようとした際の承認ダイアログがスマホの remote app に表示されない問題（ターミナル UI 描画のみで jsonl 会話データに含まれないため）の修正 Issue をプロダクト方針に合致と判定した
- **根拠**:
  - 承認ダイアログが特定環境から不可視になることは「透明性」バリュー（「全ての判断根拠をルール・設定ファイルとして可視化する。ブラックボックスを作らない」）の実態破壊に相当する。修正対象として妥当
  - `docs/specification.md` L28 に「非対応環境での既知制約」として既に文書化されているが、本 Issue はその制約を回避するための改善であり、仕様書の記述を後から更新する方向性と整合する
  - 2026-04-18 確立の「承認フロー非介入」思想との整合性確認が必要: 解決手段が「承認を自動通過させる仕組み」（team-auto-approve 類似）になった場合は NG となる。「承認を可視化・到達可能にする仕組み」であれば OK
- **条件**: 実装時に「承認フロー非介入思想と矛盾しない手段」であることを設計段階で確認すること。specification.md L28 の既知制約記述の更新要否も受入基準に含めること
- **代替案**: `.claude/knowledge/**` 等の許可済みパスを拡張して承認頻度を下げる対処は specification.md L28 推奨対処と合致しており、補完的に採用可能


## 2026-04-25 — ~/.zshrc へのシェル関数追記による --plugin-dir 自動付与を除外と判定

- **判断**: 除外（プロダクト方針に矛盾）
- **対象**: vibecorp 本体リポジトリで claude 起動時に `.claude-plugin/plugin.json` が存在すれば `--plugin-dir .` を自動付与するシェル関数を `~/.zshrc` に追加する提案
- **根拠**:
  - `docs/design-philosophy.md` の Public Ready ガードレールおよび `.claude/rules/public-ready.md` の「ローカル環境・ユーザー設定への依存禁止」に正面から抵触する。`~/.zshrc` への追記は「ユーザーのシェル設定に依存する」設計そのもの
  - MVV バリュー「透明性」（全ての判断根拠をルール・設定ファイルとして可視化する）に反する。起動引数の挙動がシェル設定に散在し、振る舞いの所在が不透明になる
  - MVV バリュー「規律の自動化」（フックとスキルで強制する）の設計方針から外れる。解決をフック・スキル層の外側（シェル設定）に持ち出している
  - install.sh による再現可能なセットアップという振る舞いとも矛盾する
- **代替案**:
  1. `Makefile` または `scripts/dev.sh` への組み込み（`make claude` で起動）— リポジトリ内で完結し透明性が保たれる
  2. `direnv` + `.envrc` の組み合わせ — スコープが限定的だが外部依存が生じるため README での明記が前提
  3. README の開発者向けセットアップセクションにコピペ可能なエイリアスを記載 — 自動化より透明性を優先


## 2026-04-25 — scripts/dev.sh ラッパーによる --plugin-dir 自動付与を OK と判定

- **判断**: OK（プロダクト方針に合致）
- **対象**: vibecorp 本体リポジトリで `claude` 起動時に `--plugin-dir .` を自動付与する `scripts/dev.sh` ラッパーの追加
- **根拠**:
  - 同日付の `~/.zshrc` 追記案（除外判定済み）の代替案1「`Makefile` または `scripts/dev.sh` への組み込み」の具体化であり、過去判断と完全に一貫する
  - `scripts/dev.sh` はリポジトリ内で完結し、ユーザーのシェル設定・マシン固有パスへの依存がない。`public-ready.md`（ローカル環境・ユーザー設定への依存禁止）に適合
  - 起動引数の挙動がリポジトリ内の可視ファイルとして追跡可能であり、「透明性」バリューに整合する
  - 開発者の不要な摩擦を排除する改善であり「導入の手軽さ」バリューに合致
  - プリセットスコープ（minimal/standard/full）の配布物に関与しないため、エンドユーザー向け仕様に影響しない
- **受入基準**:
  1. `scripts/dev.sh` に特定マシンのパスや個人情報が含まれていないこと
  2. `~/.zshrc` 等のユーザーグローバル設定を変更・参照しないこと（`.claude/rules/public-ready.md` 準拠）
  3. README の開発者向けセットアップセクションに使い方が記載されること（「透明性」バリュー）
- **代替案**: 今回の案が除外された `~/.zshrc` 方式の正しい代替であり、他案（direnv、README コピペ記載）より自動化と透明性のバランスが優れる


## 2026-04-29 — knowledge/ 構成の全ロール横展開リファクタを OK と判定

- **判断**: C*O 6 ロール・分析員 3 ロールの knowledge 構成を decisions-index + 四半期アーカイブの 2 段構成に統一し、監査ログを四半期集約へ移行、揮発データ（cycle-metrics）を `~/.cache/vibecorp/state/<repo-id>/` に撤退させる一括リファクタを OK と判定した
- **根拠**:
  - 2 段構成（index + アーカイブ）は 2026-04-18 の CPO 判断「decisions.md のインデックス+アーカイブ2段構成への分割を OK」と同一設計思想であり、全ロールへの横展開は一貫性の観点から適切
  - 監査ログの四半期集約は Issue #335 で CISO decisions.md が 631 行に達した問題（同じ末路）を構造的に防ぐ予防的設計であり、「透明性」バリュー（可視化・ブラックボックスを作らない）に沿う
  - 揮発データを `~/.cache/` に撤退させることで `knowledge/` が永続知識のみを保持する領域として明確化され、「導入の手軽さ」バリュー（構成の見通しの良さ）に貢献
  - ガードレール hook のパターン更新を 1 PR で完結させる方針は、旧パス参照の非整合状態が発生するウィンドウを排除し、「規律の自動化」バリューに沿う
- **代替案**: ロールごとに個別 Issue で分割移行する案も考えられるが、hook のパターン変更と knowledge 構成変更が別 PR になるとパターン変更ウィンドウが生じ、ガードレールが一時的に旧パスを参照し続けるリスクが発生するため、1 PR で完結する本案が適切
